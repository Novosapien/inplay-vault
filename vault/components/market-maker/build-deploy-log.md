---
description: "The cross-session working log: every in-flight change, where it is (built / merged / deployed / verified), and who or what it waits on"
---

# Market Maker & Taker — Build/Deploy Log

> **Component:** [[market-maker/market-maker]] · **Born:** 2026-08-13
> (George: one log of what is being done, where it is, and its
> implementation and deployment state).

## How to use this file — read before editing

1. **This is the WORKING LOG, not the narrative.** Session notes carry
   the why; this file carries the CURRENT STATE of every change moving
   through the pipeline: `building → PR open → merged → deployed →
   verified live`. If you build, merge, or deploy anything, update its
   row in the SAME session — a stale row here misleads every parallel
   session.
2. **One row per change.** Name the repo and PR. State the CODE state
   and the DEPLOY state separately — merged ≠ running (the recurring
   binary/repo-divergence lesson). Name what the change waits on.
3. **Deployed rows carry the running coordinates** (config version /
   journal / commit) so the next session can verify instead of trust.
4. **Move finished rows to "Landed" — never delete them.** The log is
   also the record of what shipped when.
5. **Check this file before deploying anything** — another session may
   hold an in-flight change on the same component. The taker's env on
   the VM outruns the vault regularly; the row states the last KNOWN
   coordinates and when they were read.

## In flight

| ⭐ **N55 — the MM publisher's loop tick becomes a setting.** `run_forever(..., tick_s: float = 1.0)` was hardcoded and quantised every poll tier, so the pool's `MMPUB_POLL_LIVE_S=0.5` could NEVER apply — set since 11-08, measured 29-08 at **one poll per 1.16 s** (513 fetches / 597 s on the live game). Adds `MMPUB_TICK_S` (default **0.5**) and a model validator that REFUSES a tick slower than the fastest tier, so the same silent mismatch fails at boot rather than running 19 days. ⚠ **Small by measurement, and the PR says so:** SR re-computes an NCAA probability every ~7 s (p90 25 s, min 10.1 s), so 1.16 s already oversamples it ~6×. A config-vs-code correction, **not a pricing fix**. ⏳ **Still owed and deliberately NOT in this PR:** `MMPUB_POLL_POST_GAME_S` is unset and takes the 2.0 s default — 33 finished games took ~14 of every 16 SR calls on 29-08. Env change, not code; pick the value against the MM's ~20 s freshness fuse. ⚠ Deploying restarts the publisher pool — do it with no live game | `inplay-sportradar-service` **PR #51** (`fix/mm-publisher-tick-setting`, from `main@c8d3c6e`) | ✅ built — **830 tests**, ruff + mypy clean | 🔴 **not deployed** | **George's go** (and his call on the POST_GAME value) |

| ⭐⭐ **STATE CORRECTION 19-08 — THIS LOG SAID THE GATEWAY RAN `main@a41e540` (15-08). IT DID NOT.** The VM was running a binary deployed **19-08 00:03Z**, ~20 minutes after PR #19 merged, and `/health` reports **`role: mono`** — a field that exists only in #19. So gateway `main` had been cut over to **`c4d7b94`** (PRs #8–#19: rolling wire log, ORDER DEAD retire, log diet, reject observability, adopt-unknown-orders, MM-prefix identity fallback, retail egress split, role flags) with **no row here**. ⚠ The lesson this file exists for, twice in three days: the stale row nearly turned a one-endpoint deploy into an unannounced 30-commit delivery. Rows for #8–#19 are still owed by whoever landed them | `inplay-fix-gateway-go` `main@c4d7b94` | ✅ merged | ✅ **LIVE since 19-08 00:03Z** | — |

| **Gateway `GET /positions/house` — the house book WITHOUT either engine.** The offering's read path: the panel reads the maker from `mm.state` and the taker from `snt.state.*`, both engine-published, and the maker engine is deliberately STOPPED during an offering (R-V08), so every maker surface goes blank exactly when the offering needs watching. Aggregates the tracker's MM orders per bot and symbol — ordered · sold · resting · notional · avg_px · the venue's tag 9383 — with `botId` separating **`mm-1`** from **`snt-1`** (they share the MM namespace). Also: `OrderTracker.GetMM` includes TERMINAL orders (`GET /orders/mm` lists resting only, so a sold-out line would drop off and take its sold shares with it) and `SetPosition` stores 9383/9384, which the gateway used to publish and then discard; both round-trip through the Redis order index. ⚠ Two limits, documented at the handler and covered by tests: **`pos_size` is null until a FILL** (no Request For Positions on this session — null is "the venue has not said", never zero), and after a gateway restart **`sold` is short** by whatever filled before it (the boot rehydrate replays open orders only; `resting` stays correct; Redis holds terminal orders 48h) | `inplay-fix-gateway-go` **[#20](https://github.com/Novosapien/inplay-fix-gateway-go/pull/20)** | ✅ built — 3 new tests, full suite + vet clean (`e2e/TestEndToEnd` fails on clean `main` too, pre-existing) | ✅ **DEPLOYED 19-08 ~12:20Z** — binary sha `db311940e25e06c4`, backup **`gateway-go.bak-c4d7b94-pre-house`**, both FIX sessions logged on after restart, route answers and 401s without the key | PR open for review. Session: sessions/2026-08-19-c-ipo-test-rig |

| **Panel: the `/ipo` book page** — totals (offered · sold · remaining · subscribed % · capital raised), one row per team company, filters by account and league, 10s poll. Proxy passthrough `GET /house/ipo` carries `X-Ops-Key` from a new **`GATEWAY_OPS_KEY`**, so the key stays inside the VPC; unset answers 503 naming the variable rather than calling bare and reporting the 401 as a broken gateway. Admin-only — the other `/api/house` reads opened to viewer on 14-08, but this aggregate discloses the whole house book including the taker's buying | `inplay-admin-panel-trading` **[#32](https://github.com/Novosapien/inplay-admin-panel-trading/pull/32)** | ✅ built — 5 new proxy tests, suite 236, tsc + eslint + next build clean | 🔴 **not deployed** | ⚠ **`GATEWAY_OPS_KEY` must be set on the Cloud Run proxy** or the page shows `unconfigured`. Depends on gateway #20 (already live). Session: sessions/2026-08-19-c-ipo-test-rig |

| ⚠ **ROTATE `OPS_API_KEY` on the gateway VM.** A read-only pre-deploy check on 19-08 printed `/opt/fix-gateway/.env` with a redaction pattern that did not match, so the key reached a session transcript. Nothing was written to a file, a commit or a PR. The panel's new proxy passthrough will need the new value in `GATEWAY_OPS_KEY` at the same time | gateway VM (ops) | 🔴 not rotated | — | George. Session: sessions/2026-08-19-c-ipo-test-rig |

| **The IPO test-run scripts** — `scripts/ipo/{prices,allocate,rest_orders,cancel_orders}.py` + README. Seed the maker's inventory by 35=UPT, rest one limit sell per team at the listed price, cancel at the close. Quantities come from `mm.universe` (900,000 NFL · 1,000,000 NCAA), never a flag. ⚠ **35=UPT is not idempotent and, as measured 05-08, not reversible** — positive transfers apply, negative ones are accepted (UPTa) and never move the position. So every step keeps a JSONL ledger and resumes off the **intent**, not the success, and `allocate.py` refuses to continue past a dangling intent until a human has read the gateway log. ⚠ `rest_orders.py` refuses to run without `--deadman-checked`: the dead-man has **no off flag** (`DeadManTimeout <= 0` falls back to 10s), and either a maker-engine start or a gateway restart inside the window sweeps the entire offering | `inplay-market-maker` `scripts/ipo/` — no PR yet | ✅ built — ruff clean, all three dry-run against the real 170 | — | Nothing has been sent to the venue. Blocked on the price-source decision and the UEPR reversal probe. Session: sessions/2026-08-19-c-ipo-test-rig |

| ⭐⭐⭐ **DEPLOYED 18-08 10:12Z — `main@fd193a4` (tag `mm-python-fix-set-complete`), engine `supervised41` / CFG-0038.** George's "merge them all" then "put the market maker and the taker back on". #52 (ask cap ← venue) + #54 (reject index) + #56 (W3 probe) merged after a COMBINED trial merge; gate at the pin **1,295 tests, ruff + mypy clean, run ON THE VM** before the ceremony. Boot: `replayed 1 event` · ⭐ `ANCHOR_SEED JOURNALLED 14 anchors from supervised40` · `boot heal: DONE — 10 orders, cancelled 8 unknown, left 2 taker, ALARMED 0, 19 ms` · `book standing: 1664 instructions / 180 securities`. **AC10 restored.** ⚠ **The launch mechanism has CHANGED and the docs below are stale: the engine is now `mm-1.service` with `/etc/mm-1/env` (+`env.secret`), NOT a bare `setsid nohup`** — so stdout goes to journald and `journalctl -u mm-1` is the boot log. The taker is `snt-1.service` with `/etc/snt-1/env`, and its NATS var is **`SNT_NATS_URL`**, not `NATS_URL`. Env backups: `/etc/mm-1/env.bak-cfg0037-preflight` | `inplay-market-maker` `main@fd193a4` | ✅ merged + tagged | ✅ **LIVE** — maker and taker both trading | Rollback: `/etc/mm-1/env.bak-cfg0037-preflight` + `87cb35f`. Session: sessions/2026-08-18-the-cutover-and-the-vm-hang |

| 🔴 **INCIDENT 18-08 09:41–10:12Z — THE VM HUNG REPLAYING A 3.5 GB JOURNAL. Cause: the cutover KEPT the journal.** The boot healer retires the fresh-journal *ceremony*, so keeping the journal is SAFE — but nobody had checked whether **replaying** it FITS. It did not: 3.5 GB / 3.15 M events, RSS **3.9 GB** on an n2-standard-2 (2 vCPU / 8 GB). By 09:53 systemd itself was timing out (`dbus auth elapsed: 145,927 ms`); by 10:08 `systemd-logind` could not start and the **metadata server** timed out; SSH refused (255). **Instance RESET 10:09Z.** ⭐ **No exposure at any point: the dead-man swept the book 1,588 → 10 orders the instant the old engine stopped**, the taker was already down, and it was 05:41 ET with no games live (R11 satisfied) — the failure mode was "not quoting", never "quoting wrongly". ⚠ **On reboot both units auto-started and the engine began replaying the SAME 3.5 GB journal again** (RSS 3.9 GB at 46 s) — stopped before it re-hung the box. **Recovery = the path that should have been first:** fresh `supervised41` + bump to CFG-0038 + `MM_PRIOR_RUN_DIR=supervised40` → boots in ONE event, RSS **51 MB**, and **`ANCHOR_SEED` carried the 14 anchors forward anyway, so keeping the journal had bought nothing.** ⭐ **RULE EARNED: journal size is a PRE-FLIGHT GATE on every cutover** — the healer makes keeping a journal safe, not possible; check size vs box before choosing keep-vs-fresh | — (operational) | — | ✅ recovered 10:12Z | **Owed: the pre-flight gate written into `deploy/OBSERVABILITY-REDEPLOY.md`, and that runbook corrected — it still describes the bare `setsid nohup` launch that no longer exists** |

| 🔴 **INCIDENT 18-08 13:40–16:20Z — ONE TORN JOURNAL LINE BRICKED THE TAKER for ~2.7 h.** At 13:40:32 the taker's journal stopped mid-record: `{"kind": "send", "limit_px": "76.79", "qty": 8, "seq": 294157, "side": "sell"` — **one bad line of 587,722, and it was the LAST**. The taker replays its whole journal at every start, `json.loads` throws, the process exits, systemd restarts it, and it fails again — **a ~6 s crash loop. It was never "shut down"; it could not START.** A clean SIGTERM finishes the line in flight, so something killed it hard at 13:40:32. **Repair:** backup `journal.jsonl.torn-backup-1620` → drop ONLY the torn final line → verify all 587,721 remaining lines parse → restart. Booted clean, no halt, straight to `state=AUTO`. Dropping it is correct — an incomplete write never became durable, so `seq 294157` never counted. 🔴 **THE GAP: a torn final line is the normal consequence of ANY hard kill, and it permanently bricks the taker until a human edits the file.** `snt/journal.py` replay should truncate a torn FINAL line itself (mid-file corruption must still raise). ⚠ **The Go port inherits the same replay design — this belongs in its brief** | `inplay-market-maker` `src/snt/journal.py` | 🔴 not built | ✅ repaired by hand 16:20Z | **George: build the torn-tail tolerance** |

| 🔴 **THE ORDER BOOK — investigated 18-08, two symptoms EXPLAINED, one OPEN.** George reported lopsided books, a wide spread and an empty Bengals book. Checked at the VENUE, not from the engine's own claims. **(1) Lopsided depth is BY DESIGN** — `min_levels=3, max_levels=6` drawn per side per book, so "6 bids / 3 asks" is in spec (86 of 161 books lopsided at one sample). **(2) Empty/thin books are CHURN** — the taker takes **~3.4 levels/s** portfolio-wide against a 500 ms maker pulse, so any snapshot catches half-rebuilt ladders; re-sampled 45 s later **IPTCBENG went 0 bids → 2 bids**, AZWC 0 → 2, EMEA 0 asks → 2 asks, and portfolio-wide the book was RISING (684 → 754 resting, 129 → 147 two-sided). ⚠ **But the steady state is a permanently half-empty book because the taker consumes faster than the maker rebuilds — a RATE mismatch, not a defect in either; the lever is `SNT_INTERVAL_LIVE_S`.** **(3) 🔴 OPEN: eight books persistently one-sided** across both samples — no asks on **BEAR, MIWV, NTMG, RAID, VOLS**; no bids on **CINB, GOPH, LION.TEST**. ✅ **The W2 ask cap is CLEARED as the cause:** venue positions are **96,679–104,320 shares on every book**, `IPTCBEAR` (no asks) holds **103,595**, zero books under 1,000, all `ASK_CAP_*` alarms zero. Also ruled out: venue rejects 0, suspensions 0, quarantine 0, missed sweeps 0. ⚠ **Two red herrings, do not re-chase:** `refused=19,693` is **ONE book** (IPTCPATR — our ask sits ~8c BELOW a real bid at 73.18, so the guard is right and our PRICE is wrong → E31, Edwin's); and `PUB_SHED ~98%` is **pre-existing by design** (the publisher sheds `resting_orders` to fit the 262,144-byte budget, since ≥17-08 18:04) — it means `mm.state` readers see a thin picture, which matters when a dashboard looks wrong | — (investigation) | — | — | **NEXT: the eight stuck books, then the maker/taker rate balance** |

| ⭐ **HALT ALERTING LIVE — 17-08.** The gap that turned a 4-minute glitch into a 50-hour outage is closed. `snt-halt-check` (systemd timer, 60 s) on `inplay-market-maker` reads BOTH bots' snapshots (`snt.state.snt-1`, `mm.state`) and writes 5 gauges to Cloud Monitoring; 3 alert policies email George + Hasan. **Silence pages**: the two liveness policies use `EVALUATION_MISSING_DATA_ACTIVE`, so a dead bot/VM/checker fires like a halt. Read-only end to end — new NATS user `mm-monitor` (`publish: []`), never restarts or resumes anything. Proven by injecting a synthetic `halted=1` for 11 consecutive minutes against a 5-minute threshold, then letting it clear. ✅ **DELIVERY PROVEN END TO END** (2nd drill): incident `13:03:37Z` → email in George's INBOX at `13:03:37Z`, sender `alerting-noreply@google.com`, to `george.westbrook@novosapien.ai`, not spam. The 1st drill's silence was the wrong address only — it was sent to `george.westbrook412@gmail.com`, taken from session metadata instead of asked for. (An earlier note here claimed a verification email had to be clicked; wrong — email channels are live on creation.) ⚠ **Real detection latency is ~8 min, not 5** — measured 8m22s and 8m01s from first aligned violation to incident; ingestion + evaluation add ~3.5 min to the 5-min `duration`. Do not tighten `duration` to chase it. ⚠ **THE MAKER HAS NO SUPERVISOR** — `mm.runtime` is a bare process, PPID 1, orphaned from a `screen` session, no systemd unit, so no restart on crash and no start on boot. The new policy DETECTS that; it does not fix it. A unit needs a cutover ceremony (journal + checkpoint + `ANCHOR_SEED`) — **owed before 29-08**. ⚠ Also found: `VM root disk > 80% (fix-gateway, market-maker, nats)` never covered the market-maker VM — it filters `agent.googleapis.com/*` and no ops agent was installed there. Now installed (side-loaded `.deb`; the VM has no internet egress) | ops: `inplay-market-maker`, `inplay-nats` | — | ✅ **LIVE**, drill-verified | the maker's systemd unit; a `mm/quarantined_books` threshold (N40) |

| ⭐ **TAKER RECOVERED + THE 40 s RATE CLEARED — 17-08 10:44Z and 11:0xZ.** The taker had been HALTED 50 h (since 08:17:13Z 16-08). Recovery ran the fresh-journal ceremony at 10:44Z: **`SNT-CFG-0026` / journal `/var/lib/mm/snt23` / `OVERNIGHT=120`** (env backup `.bak-2026-08-17-cfg0025`) — 177 boot rebases, 546 fills in the first minutes, zero halts, zero `CANCEL STUCK`. George then asked WHY 40 s had failed rather than accept 120 s as a guess, and the measurement cleared it: a per-minute pass over the full 4.6 GB FIX wire log shows **0.02–0.04 s mean inbound lag in every hour at 40 s**, **286 msg/s at 0.04 s** in the busiest hour, and **6 lost fills in 230,847 (0.0026%)**. Rate returned to 40 s at 11:0xZ: **`SNT-CFG-0027` / journal `/var/lib/mm/snt24` / `OVERNIGHT=40`** (env backup `.bak-2026-08-17-cfg0026`), verified from `/proc/PID/environ`, zero halts. ⛔ **The 17-08 "gateway 17–27 s behind" root cause is WITHDRAWN** — that reading was sampled inside a 36-message minute during a dead FIX session. Real cause: a FIX session break ~08:13 on 16-08 recovered by a `ResendRequest` at 08:17:16. ⚠ The gateway discard path is NOT proven (`:474` is a candidate; the dedup paths at `:497–517` are alternatives) — instrument before fixing. Other live posture unchanged: `SNT_LOSS_BUDGET=0` · `SNT_MAX_SPREAD_TICKS=40` · `SNT_MAX_ORDERS_PER_S=0` | `inplay-market-maker` (ops only, no code change) | — | ✅ **LIVE**, verified | §3.1 halt alerting; gateway discard-path counters + journald retention on `inplay-fix-gateway` |


| ⭐⭐ **THE FIX SET IS PROVEN LIVE — the observation night, 15/16-08.** Engine **supervised35** (chain 33 → 34 → 35, ~13 h): **92,111 due-sweep ticks, ZERO missed** (AC4's live clause is < 1%; we measured 0.0000%), **1 DRAIN_CAPPED** (boot), **0 guard-stall alarms**, 3,201 guard refusals doing their job, one engine throughout. ⭐ **THE ANCHOR CHAIN HELD ACROSS THREE CONSECUTIVE CUTOVERS** — 33→34 carried **10 anchors** (checkpoint + 531 tail events); 34→35 carried **12 anchors** from a run with **NO usable checkpoint** ("the whole prior journal is the tail", 4,919 events folded). That second hop IS review-002's B1 scenario exactly — a second cutover with no checkpoint, where the prior run's own `ANCHOR_SEED` must be read back — and it worked because the B1 fix put `ANCHOR_SEED` in `_ANCHOR_TYPES`. The defect that erased $0.685/share on BENG is closed, in production, in the shape the reviewer constructed. ⚠ AC4's DRAIN_CAPPED clause still reads "zero" and is mis-specified (boot re-stand only — measured by tick index across 5 rig arms + live) — George's one-line ruling owed | `inplay-market-maker` | ✅ merged | ✅ **LIVE and PROVEN** | George: the DRAIN_CAPPED clause ruling; N43 (ask cap activation); `MM_GATEWAY_OPS_URL` (boot healer still inert) |

| ⭐⭐⭐ **MM-PYTHON-FIX-SET-COMPLETE — EMITTED 18-08. THE GOSPEL COMMIT IS PINNED: `fd193a4`**, tagged **`mm-python-fix-set-complete`** (pushed). George's "merge them all". #52 (ask cap ← venue), #54 (reject index), #56 (W3 probe + verdict) all MERGED to main after a combined trial merge proved they land together. **Gate at the pin: 1,295 tests, ruff clean, mypy src clean, mypy tests 113 = the standing baseline** — verified on a scratch checkout of `origin/main`, not on a branch. 27 commits since the fix set closed at `0b9f601`. ⚠ **THIS IS THE REFERENCE COMMIT THE GO PORT CERTIFIES AGAINST** — the Phase-0 artefacts (2,285 decimal vectors, the reference checkpoint) were generated at `662b032` with a dirty tree and **must be regenerated at `fd193a4`** before certification; both are one command (`scripts/go_conformance_vectors.py`, `scripts/go_reference_checkpoint.py`). ⚠ **NOTHING DEPLOYED.** The running engine is still **`87cb35f` / supervised40 / CFG-0037**; main and running differ by **528 insertions across six source files** (`gateway.py`, `orchestration/engine.py`, `sellable.py`, `venue_holding.py` NEW, `compose.py`, `backoff.py`). R11 holds | `inplay-market-maker` `main@fd193a4`, tag `mm-python-fix-set-complete` | ✅ **MERGED + PINNED** | 🔴 **not deployed** — running is `87cb35f` | **George: the cutover, when he wants one.** ⚠ It must carry the boot-log redirect (now in the runbook) or AC8 stays unverifiable. Session: sessions/2026-08-17-mm-pre-port-close |

| ⭐⭐ **STATE CORRECTION 17-08 ~21:4xZ — THE LOG BELOW WAS STALE BY NINE CONFIG VERSIONS.** A parallel session drove the VM from CFG-0028/supervised33 to **CFG-0037 / journal supervised40 on `main@87cb35f`** (booted 21:28:17Z; branch `mm-main-87cb35f`), landing PRs **#45 #46 #49 #50 #51**. None of CFG-0029→0037 or supervised34→40 had a row here. `main` moved AGAIN mid-session to `1bf4479` (#50). **Engine health on it: ZERO missed sweeps in the run**; 1,588 MM orders resting; `MM_PRIOR_RUN_DIR=/var/lib/mm/supervised39` (a **1.046 GB** journal) and ANCHOR_SEED journalled its seed as event 1. ⚠ **The lesson this log exists for, repeating:** a stale row here misleads every parallel session — the brief for this session was written against `0b9f601` and was wrong about the running state before work began | `inplay-market-maker` `main@87cb35f` → `1bf4479` | ✅ merged | ✅ **LIVE** (supervised40/CFG-0037) | Rows for #45/#46/#49/#50/#51 are still owed by whoever landed them |

| 🔴 **THE ENGINE'S BOOT LOG IS NOT DURABLE — and it is what AC8's verification depends on.** The running engine's stdout is a **socket** (`/proc/303211/fd/1 -> socket:[933655]`), not a file: supervised37–40 have **no log on disk**, where supervised27–36 all do. The `> ~/supervisedNN.log 2>&1` redirect was dropped from the cutover ceremony. W1's verification step is literally "verify in the boot log: `boot heal: DONE — n MM orders at the venue · cancelled x unknown + y absent`" — **that line is unrecoverable for the 21:28Z boot**, and will be for every future one until the redirect returns. A ceremony regression, not a code defect. ⚠ This is rule 1 ("any job over a few minutes emits a heartbeat, or it is not observable") biting the very deploy that was meant to close AC8 | — (ops/runbook) | 🔴 not fixed | — | **George: put the redirect in the runbook as a mandatory cutover step.** Until then AC8 cannot be verified on any cutover |

| ⭐ **W1 — THE BOOT HEALER IS CONFIGURED AND LIVE, and its ownership boundary is now PROVEN ON PRODUCTION IDS (read-only, 17-08).** `MM_GATEWAY_OPS_URL=http://10.0.1.2:8080` and `MM_GATEWAY_OPS_KEY` are **already set on the running engine** (set by the parallel session, not this one). Route verified independently from the MM VM: **HTTP 200**, well-formed `count` + `orders`, 1,588 entries, 56 ms. The **real classifier** (`plan_boot_heal`) was then run read-only over that live index: **1,587 ours (`MM` + 16 hex) · 1 taker (`MMSN`) · 0 ambiguous · 0 foreign · 0 malformed** — the `MM`/`MMSN` boundary the design called "thin until written down" holds on real ids, and the taker's order is classified TAKER and never touched. Two plan arms: record EMPTY (fresh journal) → **1,587 cancels planned** (the ceiling); record knows them → **0 cancels**. ⭐ **This answers the drill's own open question — and CB4 is what makes the healer safe.** The heal's return traffic is one venue event per cancel against the 512/tick drain cap: 1,587 ÷ 512 = **4 ticks**; at CB4's measured **0.298 ms/ack** that is **153 ms per tick** (473 ms total), inside the 500 ms tick — but at CB1's pre-CB4 **9.893 ms/ack** it would be **5.07 s in ONE tick**, past the beat-stall threshold, arming the dead-man on its own boot. ⚠ **Still owed: the AC8 rig drill was NEVER run, and the live cutover happened anyway** (the brief says drill first). ⚠ Still owed: a cutover that KEEPS the journal — 21:28Z took a fresh one (supervised40), which is the arm where the healer cancels everything | `inplay-market-maker` — no code change (W1 is config) | ✅ on main since the fix set | ✅ **LIVE AND VERIFIED** ✎ 18-08 — by the parallel session's run (vault `main@70089fc`, MM #30), not this one. ⚠ **The real blocker was never the env var: `inplay-fw-policy` is a NETWORK firewall policy, evaluated before VPC rules, default-deny both ways.** Their first run: `boot heal: DONE — 1646 MM orders, cancelled 1645 unknown, kept 0 known, left 1 taker + 0 foreign, ALARMED 0 ambiguous` — **1,645 orphans from every pre-healer restart**, cleared in 62 ms, and my read-only dry-run had predicted 1,587/1 taker/0 ambiguous | George: the drill, and the boot-log redirect. Session: sessions/2026-08-17-mm-pre-port-close |

| ⭐⭐ **W2 — THE ASK CAP READS ITS POSITION FROM THE VENUE (R-Q08 / AC7 / E27's maker half).** George's 17-08 ruling: do not wait for Edwin. **Measured before building:** tag 9383 reaches the maker on **212/212** exec reports (100%), **per symbol** across 140 securities, and **133/133 consecutive deltas match our own fills exactly**; maker account `1797733477` ≠ taker `4963224393`. ⭐ **The venue says we hold 59,277–106,225 shares per book while `opening_position_shares` is a 0 stub** — R-Q08 has been failing open against a real ~100,000-share holding all along. Three seams: the adapter renames `posSize`→`venue_position` **only when present** (absent is never zero); `position/venue_holding.py` is the **FIFTH** state-set question and gets its own module; `sellable.ask_capacity` takes the venue figure as the holding **whole**. **Deviation, argued:** the LATEST figure, not the taker's FIRST — the cap keeps no tally to defend, and riding the already-journalled EXECUTION envelope makes replay reproduce the fold **by construction** (venue-BORNE, not venue-LIVE — the N45 distinction; AC9 proven by a live-vs-replay byte-equality test that also asserts the holding is IN the state). **`0` binds, `None` does not.** ⭐ **Deploy safety, measured against the live book: activating it changes NOTHING today — 0/119 books empty, 0/119 resized**, tightest book 52.6% of holding, p50 25.9%. Proven end-to-end on **180 real captured production exec reports**: 180/180 carried the figure, fold agrees with the venue on all 119 securities, zero mismatches. Kill switch `MM_ASK_CAP_VENUE=off`. **1,278 tests, ruff clean, mypy src clean, mypy tests 113 = baseline exactly; 3 mutations checked.** ⚠ Known gap: our livS is OURS, the venue's is the ACCOUNT's — **the cap and the F4 healer are load-bearing for each other** | `inplay-market-maker` **[#52](https://github.com/Novosapien/inplay-market-maker/pull/52)** (`feat/ask-cap-venue-position`, base `main@87cb35f`) | ✅ built — 1,278 tests, ruff + mypy clean | 🔴 not deployed | 🔴 **N43 rider (3) `[post-first]` ordering — the last gate before activation.** Then review + George's explicit go; R11 gates timing |

| ⭐ **W4 — THE REJECT TABLES ARE KEYED BY SECURITY, and the real finding is the BLIND SPOT.** `RejectBackoff.suppression()` filtered the WHOLE portfolio's two tables to answer a per-security question, per security examined, per converge pass, at 4 Hz. ⚠ **The six-game rig structurally CANNOT see it** — the synthetic venue never rejects, so both tables are empty for every run ever done; production agrees while healthy (supervised40: **0 order rejects, 11 cancel rejects** in 37 min). So the PR ships the workload that can produce the input: `six_game_workload.py --reject-rate / --cancel-reject-rate`, deterministic per ORDER ID (sha256 draw, never a shared RNG, so it cannot depend on async arrival order), **both defaulting to 0.0 so every existing arm is unchanged** — verified: stock arm 6,120 answers / **0** rejects, 2% arm 5,922 answers / **40** order + **15** cancel rejects. **Measured → changed → re-measured, adjacent arms in one process:** at the production ceiling (1,800 price + 1,643 cancel entries, 64 securities/pass) **3.762 → 0.429 ms/pass p50, 8.8×**, 1.50% → 0.17% of the 250 ms interval. The SHAPE is the point — ms/pass at 0 / 1k / 4k / 8k / 16k entries: flat **0.027 / 1.707 / 4.498 / 8.634 / 29.066**, indexed **0.029 / 0.222 / 0.504 / 1.022 / 1.546**. ⚠ **The 16,000 column is the 15-08 disk-full incident** (a dead FIX session locally rejecting every order, ~500/book): the old shape spent **11.63%** of every converge interval on this scan at exactly the moment the venue was in trouble. **NO CHECKPOINT BYTE MOVES** — a test reimplements the pre-index rendering as a reference and compares canonical JSON, so AC9 is untouched. A filter-a-global-table-by-key scan takes an INDEX, deliberately not the prune queue that fixed `_stamp_and_prune` (an expiry problem, wanted a heap) — three shapes, three fixes, see `scan-sweep.md`. **1,263 tests, ruff clean, mypy src clean, mypy tests 113 = baseline; 3 mutations checked** | `inplay-market-maker` **[#54](https://github.com/Novosapien/inplay-market-maker/pull/54)** (`perf/reject-backoff-index`, base `main@1bf4479`) | ✅ built — 1,263 tests, ruff + mypy clean | 🔴 not deployed | Review + George's explicit go; R11 gates timing |


| ⭐ **THE FIX-SET CUTOVER — DEPLOYED 15-08 19:55–20:00Z** (`main@0b9f601`; George's explicit go, **R11 EMERGENCY OVERRIDE — three games were live**, accepted knowingly because IPTCPANT had been wedged/dead ~50 min with no operator lever). Engine **supervised33/CFG-0028**, taker **SNT-CFG-0024 / journal snt21** (env backup `.bak-cfg0023`). ⭐ **ANCHOR_SEED WORKED ON ITS FIRST LIVE CUTOVER: 6 anchors carried** (checkpoint seq 1,292,029 + 549 tail events) — the live games' kickoff probabilities survived the restart instead of being erased, the defect this fix set exists for. ⭐ **PANT UN-WEDGED: 59 fills in the first 2 min** (dead since 19:10Z). First 300 ticks: **0 MISSED_SWEEPS, 0 DRAIN_CAPPED**, 1,602 instructions standing / 180 books, `refused=71` (R-Q09 guard live and refusing), 359 taker fills, zero halts, one engine (lock held). ⚠ Boot healer INERT — `MM_GATEWAY_OPS_URL` unset, failed open loudly as designed; the fresh-journal ceremony is still owed until it is configured. ⚠ Ask cap DARK (`ASK_CAP_UNBOUNDED`, E27/N43). Rollback: `7ca9d76` + fresh journal | `inplay-market-maker` `main@0b9f601` | ✅ merged, 1,236 tests | ✅ **LIVE** | — |

> ⛔ ~~**DEPLOY FREEZE — George, 14-08 ~22:30Z: "don't deploy anything after
> now."** Nothing deploys to ANY component (engine, taker, gateway,
> publisher pools) until he lifts it.
> ✎ **15-08 02:47Z: George's explicit in-session go superseded the freeze
> for the GATEWAY** — the TTL binary (#5 + #6, `main@a41e540`) deployed in
> the post-slate window (see Landed). Engine/taker restarted on UNCHANGED
> code as part of the ordered ceremony. The freeze STANDS for everything
> else (MM #36, publisher #38 → prod, the fix-set stack) until George
> lifts it. The running stack now (✎ 22:49Z 15-08):
> engine **supervised34/CFG-0031** @ `main@006eb96` (the #45
> settled-echo guard cutover, 22:46–22:49Z, George's "deploy this
> change now"; supersedes supervised33/CFG-0028 @ `main@0b9f601`,
> 19:55–22:46Z) · taker **SNT-CFG-0024 / journal snt21** @
> `main@0b9f601` (plain systemctl stop/start around the engine
> cutover — same binary, env, journal; filling on restart) ·
> gateway **`main@a41e540`** (TTL build, sha `737a6888…`) ·
> publisher testing pool @ `d492dcb`.

| Change | Repo / PR | Code state | Deploy state | Waits on |
|---|---|---|---|---|
| **POST_GAME starts at the final's RECEIPT (`[final-is-receipt]`)** — `final_time` was stamped from the minting reading's provider stamp (the last PROBABILITY move), hours before the status flip on a settled game: measured 15-08, a final received 23:14Z carried stamp 22:33Z (40 min of the 1 h window pre-spent), another 17:05Z (POST_GAME never visible — George's report). George's ruling: the book switches to POST_GAME when the game IS finished → `final_time = env.receive_time` (journalled, replay-identical). No schema change. 1,241 tests, ruff + mypy-strict clean. ⚠ Residue stays open: a fresh-journal cutover forgets `final_time` until the next re-offer re-mints the final — belongs to the auto-recovery design | `inplay-market-maker` **#46** — merged as `main@d2b2fb5` | ✅ merged | ✅ **DEPLOYED 15-08 23:37–23:39Z — supervised35/CFG-0032, fresh journal, George's go.** Ceremony atomic (one remote script): taker stop → engine SIGTERM (tick 5,888) → 20 s sweep → checkout `mm-main-d2b2fb5` → boot: ANCHOR_SEED **12 anchors** from supervised34 (no checkpoint yet — whole journal as tail, 4,919 events, lenient reader as designed), replayed 1, **books=180** → taker restarted, active. Env `~/.engine.env.cfg0032`. The running stack header above is superseded: engine **supervised35/CFG-0032** @ `main@d2b2fb5` since 23:39Z | Verify POST_GAME on the next finals; the fresh-journal `final_time` residue rides the auto-recovery design |
| ⭐ **THE SETTLED-ECHO GUARD (N40's engine half): a final must survive late readings.** Journal-verified on supervised33 (all six 15-08 books): `OFFICIAL_RESULT` accepted at each whistle (20:01–20:04Z) set `live=False` correctly — then SR's post-final SETTLED readings (p=1/0, ~2 min later) re-armed the live regime through `_note_freshness`'s unconditional branch; the books rode the publisher's post-game watch ~2 h and suspended 20 s after it expired (22:01–22:05Z). The 13/14-08 N40 incidents share the shape. Fix `[settled-freshness]`: `_FreshnessState.settled_game_id` pins the final to ITS game — a matching reading updates `last_reading_time` only; the next fixture (new game_id) arms normally; **checkpoint schema 6 → 7** (old checkpoints full-replay). 4 new tests (incident sequence end-to-end, next-fixture re-arm, replay identity, checkpoint round trip); **1,240 tests, ruff + mypy-strict clean.** ⚠ Makes publisher #38 a mitigation, not the cure — without #45 keep-polling holds finished books in the LIVE regime at pre-final prices (the COMM/DOLP shape). E11 (deliberate post-final policy) still open. Session: [[market-maker/sessions/2026-08-15-settled-echo-guard]] | `inplay-market-maker` **#45** — merged as `main@006eb96` | ✅ merged (George's go), 1,240 tests | ✅ **DEPLOYED 15-08 22:46–22:49Z — supervised34/CFG-0031, fresh journal, George's explicit "deploy this change now" (freeze superseded for this change; no games live, R11 clean).** Ceremony: taker `systemctl stop` (no journalled halt) → engine SIGTERM (tick 20,206, clean) → 20 s dead-man sweep → checkout `mm-main-006eb96` → boot: **ANCHOR_SEED journalled 10 anchors from supervised33** (schema-6 checkpoint read leniently + 531 tail events), replayed 1, **books=180** (was 176) → taker restart (same binary/env/journal SNT-CFG-0024/snt21, filling, no halt). **ALL SIX SUSPENDED BOOKS RECOVERED, two-sided at SETTLED prices** (BILL 79.92/80.29 · BEAR 71.10/71.31 · VIKI 65.59/65.72 · BROW 44.63/44.86 · PANT 53.11/53.30 · GIAN 55.89/56.11 — winners up, losers down). Env `~/.engine.env.cfg0031` (captured from the running process, 3 vars patched). ⚠ Boot healer still INERT (`MM_GATEWAY_OPS_URL` unset — inherited). Rollback: `main@0b9f601` + fresh journal | Verify tomorrow's slate end-to-end (final → echo → guard holds → books stay up); then the E11 policy + the auto-recovery design George asked for |
| 🔴 **THE STALE-PRICE BUG FOUND (15-08 ~21:00Z audit): the trading worker's quote fold runs ~40 min behind during games — this is George's "last price vs mid on every team" AND Jared's 14-08 item #6.** The app's team-page prices ride the `market:board.{league}` Centrifugo channel; at 20:53Z its newest publication carried rows stamped 20:18 (34.5 min stale), 44 min by 21:12Z and growing. `market:book.*` is seconds-fresh (different path), so book right + team price old = the visible gap, worse the faster prices move. Mechanism (`inplay-trading-service` `src/app/services/market.py`): the worker consumes core NATS `market.>` (queue `trading-market`, **no JetStream consumer — the backlog is INSIDE the process, invisible to bus metrics**, which is why the earlier session cleared the market data path) and `_handle_message` folds EVERY quote/trade with a full DB session + commit, sequentially. Measured input in-game: **~103 DB-bound msgs/s** (quote 94.9 + trade 8.2; book 73.5/s more on the same queue, cheap path) vs 2 worker instances (min-scale 2, 1 vCPU) draining far less. Backlog grows all game, drains overnight → symptom vanishes by morning, returns every game night. The taker's 1 s rate exposed it, did not cause it. ⚠ nats-py default pending = 512k msgs / 128 MiB, no error_cb — a deep backlog can start dropping NEW messages silently (candle/volume holes); not observed tonight, a risk. ⚠ NCAA Saturday ≈ 10× input. **Fix shapes:** conflate the fold (newest row per ticker on an interval; trades are only 8% of volume) or move it behind a JetStream consumer so lag is visible. **Emergency lever: a worker restart drops the in-process backlog — prices go fresh at the cost of the backlog window's candles/volume.** P&L reads the same rows → "P&L always off" is the same bug. Session: sessions/2026-08-15-taker-timing-audit ✎ **FIX BUILT same night (George's go): [trading-service PR #3](https://github.com/Novosapien/inplay-trading-service/pull/3)** — the conflated fold (callback merges in memory, one transaction per 0.5 s pass; trades lossless for tape/candles), a monotonic `source_ts` guard on the row (an older message can never overwrite a newer price — kills the backwards churn a second session traced), the fold-lag WARNING + a NATS `error_cb`. 13 new tests; suite parity with main (the 16 `test_venue.py` fails pre-exist on a fresh test DB, verified by stash/run) | `inplay-trading-service` **[#3](https://github.com/Novosapien/inplay-trading-service/pull/3)** — **MERGED to `main@4875ead`** (George's "push it now", 15-08 ~21:41Z) | ✅ merged, 510 passed | ✅ **DEPLOYED 15-08 21:44–21:46Z on George's explicit go** — image `market-fold-20260815`, **worker rev 00053-df4 + api rev 00065-bl4**, health green. **VERIFIED: the board channel's sourceTs went from ~44 min stale to 3–7 s**; `venue quote fold started (every 0.50s)` on both instances; boot-window lag warnings (11–31 s during hydration) cleared within ~2 min, then zero warnings, zero exceptions. Rollback: redeploy image `mm-filter-volume-20260815`. Quiet window (games ended ~21:00Z); maker/taker untouched by design and unaffected | ⚠ Watch the FIRST live-game window on this build: the fold-lag WARNING is the tell. Post-merge code review in flight; findings (if any) patch on top. Follow-ups still open: POSITION DIVERGENCE flood (very visible in the new rev's logs), lb-engine's own `market.>` consumer | The audit also CLOSED the taker-rate residual: 1.012 s arrivals ÷ (1−wash skip) ÷ per-LIVE-book fill ratio 0.87–0.89 (portfolio 94% masked it) = 1.32 s predicted vs 1.27–1.35 s measured; the 37/min low end was the 20:00–20:12 HALT/IPTCAFFC-reconcile/restart window. `SNT_INTERVAL_OVERNIGHT_S=40` verified realised (41.3 s mean) |
| ⭐ **THREE-GAME PROBABILITY REPLAY RUNNING on the MM VM** (the 14-08 paced-replay decision, extended to three games / six books): PIT@CIN `sr:sport_event:50128577` → BENG/STEE · IND@NE `50128583` → PATR/COLT · GB@DET `50128599` → LION/PACK. Three `mm_prob_replay.py` processes in `~/prob-replay/` (own venv, service `src/app` copy, fixtures in `captures/`), publishing on the real bus as the `sportradar` NATS user (`inplay-mmpub-nats-url`). Verified 18:23Z: readings in the supervised28 journal, all six books repricing. Teams chosen = the played 13-08 slate — zero overlap with the real 14–16 Aug games. **THE CANCEL LEVER: `~/prob-replay/stop-replays.sh`** (SIGTERM all three, pidfile-tracked; books staleness-suspend ~20 s later — safe; restarting the replay recovers them; **drilled live 19:0xZ**). Restart: `~/prob-replay/start-replays.sh`. ✎ **Re-offer patch (~19:0xZ):** the first run staleness-suspended PATR/COLT (timeline gaps > 20 s, no re-offers — decisions 2026-08-14e); the script now republishes the last reading every 2 s (`--reoffer`, default 2.0) and all six books hold `defensive`. ⚠ The patched script is UNTRACKED (`local/replay-sandbox` + the VM copy) — PR owed if kept | — (operational; nothing deployed, no engine/config change) | — | ✅ **STOPPED ~19:4xZ 14-08 on George's instruction** (clean `replay_stopped` in all three logs; ✎ honesty note: the processes were ALREADY down when the cancel ran — stopped minutes earlier by another hand, George or a parallel session; zero replay processes confirmed). The six books staleness-suspend ~20 s after the stop — expected, safe. Ran 18:22Z → ~19:4xZ | Well clear of the 23:00Z real slate. Restart if ever wanted: `~/prob-replay/start-replays.sh` (the whole rig stays on the VM) |
| **TONIGHT'S PRE-SLATE PREP (14-08 evening, George's rulings)** — (1) the three `mm_prob_replay` test-game processes KILLED ~21:05Z (tonight = REAL games; the standing revert rule); the all-DEFENSIVE panel was their load tripping the portfolio-wide missed-sweep counter. (2) Sweep tolerance **1.0 → 2.0 s** built (`hotfix/sweep-tolerance-2s@57765ff`, one line off the RUNNING commit db45300; 881 tests). (3) **F5 CORE DONE: engine `main@ed921ca` = the running lineage + the hotfix**; `feat/always-quoting-step4b` fast-forwarded to the same commit — one lineage now. ✎ CORRECTION on the earlier note: the running engine was NEVER on 0.625 — db45300 carries the 08-13 1.0 s ruling; the 0.625 sighting was a stale local branch | `inplay-market-maker` main `772e79c → ed921ca` | ✅ merged — 885 tests, ruff + mypy clean on main | ✅ **DEPLOYED 14-08 22:12Z — supervised29/CFG-0027 on `main@ed921ca`** (George's explicit go ~22:00Z, ~1 h pre-kickoff). Ceremony: taker stop 22:10Z → engine clean stop (tick 55,974) → double cancel_all (flushed) → fresh journal `/var/lib/mm/supervised29` (replayed 0) → boot clean, **1,660 instructions stood / 180 books**, one boot-transient CONVERGE_STALE then backlog ~0–3 → taker restarted 22:13:56Z, **filling from 22:14:01Z**, no halts. Census: ONE engine (lock held). Code shipped via bundle `/tmp/mm-ed921ca.bundle` (VM origin is a bundle, no live remote). ⚠ Ceremony deviation, recorded: the journalled taker HALT could not be published — `snt.control.snt-1` refused the market-maker user's publish SILENTLY (no grant; verified by absent log line, per the runbook's own verify-by-observing rule) and no credentialed publisher exists on the MM VM — fallback was stop-without-halt with the GLOBAL cancel_all sweeping any taker stray (same MM namespace). **Ops gap: put a control-lane credential on the VM** | — |
| ⭐⭐ **DEPLOYED 15-08 17:00–17:04Z — BOTH, on George's repeated "deploy now" (EMERGENCY cutover under R11: games were LIVE; mirrored here + session note).** **Taker:** SNT-CFG-0022/snt19 at 17:00:49Z (stop-without-halt — no nats CLI/control cred on the VM; boot rebase adopted venue figures on every book), then **SNT-CFG-0023 / journal snt20** at 17:03:53Z after the engine cutover; code `snt-checkout` → `mm-main-7ca9d76` (= `main@7ca9d76`, #40); 55 fills in the first 30 s, no halt. **Engine:** clean stop of supervised31/CFG-0029 (tick 3,996, 17:02:53Z) → 15 s dead-man sweep as the cancel_all → `~/inplay-market-maker` → branch `mm-main-eng` (= `main@7ca9d76`: #36 integers · #39 prune · #32 ANCHOR_SEED · #34/#37 guard · #33 · #35 · N45 fix · #38 dark) → **supervised32 / CFG-0030** booted 17:03:10Z, **1,584 instructions / 180 books, ANCHOR_SEED journalled 4 anchors from supervised31, `MM_MARKETABLE_GUARD=off` for tonight (lead's call — no drill, first live night), replayed 1, zero errors, ONE engine (pid 243807).** Env captured at `~/.engine.env.cfg0030` (600) from the running process; log `~/supervised32.log`; the bundle `/tmp/mm-main-7ca9d76.bundle`. Note: the engine runs as a bare `setsid nohup` process, not a unit. Vault sessions/notes: 2026-08-15-qty-grid-100 |
| ⭐ **TAKER LIVE RATE — PR #40 MERGED 15-08 ~17:40Z (main `7ca9d76`), George's "get this done now".** One print per book every 20 s PRE / **1 s LIVE** / 20 s POST (multipliers 20/400/20, was 6/75/4; OVERNIGHT untouched); the arrival-clock bias fix (`schedule_after_arrival`, gaps measured from the arrival not the tick — realised LIVE 1.012 s at the new 0.25 s tick); tick 0.5 → 0.25 s (`SNT_TICK_S`); portfolio cap `SNT_MAX_ORDERS_PER_S` **ships OFF** (N44); rates env-tunable `SNT_INTERVAL_{OVERNIGHT,PRE_KICKOFF,LIVE,POST}_S`. **Lead's fast review (read of the whole taker diff + 1,173 tests / ruff / mypy on the merge): no BLOCKER/HIGH.** Notes: (1) the shared RNG + rotating scan start means the same seed no longer assigns the same draws to the same books as pre-#40 — irrelevant in practice (every restart bumps `SNT_CONFIG_VERSION`); (2) after a stall the clamp allows at most one arrival per book per tick (4/s ceiling), no burst; (3) `SNT_TICK_S=0` is unvalidated → a busy loop — leave it unset; (4) a typo'd env NAME is silently ignored (standard). ⚠ Book-visible (fill cadence) — 🟡 GEORGE, Edwin's E41 round. ⚠ Maker-load coupling: each fill is a maker exec ack; ≤ 6 LIVE books ≈ +5% maker tick — fine for a small slate; belt-and-braces `SNT_MAX_ORDERS_PER_S=10`. The PR's own "rig run at LIVE rate against the maker" box is unticked — accepted risk on George's instruction. ✎ **THE FULL REVIEW LANDED after the merge (rev-pr40, probes under `scratchpad/probes-pr40/`): MERGE-WITH-FIXES, no blocker — but two HIGHs George must see BEFORE the taker deploy.** **HIGH-1:** the 1 s LIVE interval is now SHORTER than the 1.5 s IOC cancel window, so an own unfilled remnant blocks the reversing side on most arrivals — realised gap **1.02 s (instant fills) → 1.51–1.63 s (1.5–1.8 s time-to-terminal)**, wash-guard skip **9% → 33–39%**; the "~10%" band in decisions 2026-08-15e is the OLD rate's number; the PR's tests measure the bare agent (arrivals), never sends. Levers if the rig says slow: `ioc_cancel_after_s` (1.5 s, set when arrivals were 5.3 s apart) or the guard reading `leaves_qty`. **HIGH-2 (BOOK-VISIBLE):** the guard removes exactly the REVERSING side, so at a high skip rate the taker's flow stops being 50/50 noise — P(next print same side) **0.50 → 0.75–0.80, longest same-side run 13 → 25–35 prints** — Edwin sees directional runs on the tape; belongs in E41. **MEDs:** `SNT_TICK_S` unvalidated (0 → busy loop; with cap ON → zero trading); no boot line prints the effective rates/tick/cap (typo'd env NAME = silent defaults); rotation runs with cap OFF (draw→book assignment differs from pre-#40 — harmless). Also: with the cap ON it caps ARRIVALS not sends (binds ~1.5× tighter than N44's arithmetic); `SNT_INTERVAL_LIVE_S` is itself a portfolio throttle | `inplay-market-maker` **#40** | ✅ merged (`7ca9d76`) | ✅ **DEPLOYED 17:03:53Z — SNT-CFG-0023 / journal snt20** (`snt-checkout` @ `mm-main-7ca9d76`) | ⭐ **VERIFIED LIVE 18:2xZ over the first 76 min (6 books LIVE on three preseason games; read-only, session 2026-08-15-taker-rate-verification).** **The rate lands.** LIVE **send** gap **1.13–1.16 s** per book (target 1.0 s; arrivals ~1.01 s, ~11% skipped) · LIVE **fill** gap mean 1.16–1.25 s, median 0.77–0.88, **p90 2.6–2.8 s = mean × ln 10 → the gaps are still exponential, T-F01 intact** · **OVERNIGHT 394 s vs the 400 s target** (the arrival-clock fix proven at both ends) · portfolio 5.70 sends/s. **Health: 25,983 sends · 24,534 fills (94.4%) · ZERO rejects · zero halts · zero reconcile alarms · zero resting orders at sample (316 filled + 13 cancelled in the 60 s screen linger → T-O03 holds).** Flow: buy fraction **49.6%** portfolio (48.4–51.5% per LIVE book), clip mean **44 sh** (Edwin's ~44). Cost **$2,507–2,601/hr per LIVE book** → ~$8,900 over a 3.5 h game vs the $100k budget (9%). ⭐ **BOTH review HIGHs measured, and both are far milder than the predicted band:** HIGH-1's realised gap is **1.13–1.16 s, not 1.51–1.63 s**, and the wash-guard skip **~11%, not 33–39%** · HIGH-2's directionality is **P(same side) 0.542** (24,961 LIVE sends; per book 0.528–0.553, longest run 14–18) against the predicted **0.75–0.80 / runs 25–35**. Mechanism: time-to-terminal is near-instant on a deep maker book, so the 1.5 s IOC window rarely overlaps an arrival — the HIGHs' arithmetic was right, its input (1.5–1.8 s to terminal) was not. **Maker unhurt: supervised32 has ZERO `MISSED_SWEEPS` in 9,505 ticks**, 3 `DRAIN_CAPPED`; drain 88.9 ev/tick and sent 53.1 (vs 16.9/16.9 in supervised30's pre-game tail — that window has three live games too, so it is not taker-attributable). ⚠ **ONE REAL FINDING — see the row below (float vs the soft cap, IPTCBEAR).** Still unrun: the `SNT_MAX_ORDERS_PER_S` arm of TT9 |
| ⭐ **THE SIMULATION POSTURE — George, 15-08: "very low limitations, we just want the taker to be very active".** Every taker limit is now a DEPLOY decision, not a code one; the code defaults are unchanged (book-visible numbers stay Edwin's until E41). The four env rows, in `deploy/snt-1.env.example` and the runbook: **`SNT_LOSS_BUDGET=0`** (no loss governor — Edwin's 15-08 ruling; it bound nowhere anyway, busiest book $5,811 of $100,000) · **`SNT_MAX_ORDERS_PER_S=0`** (no portfolio cap — **George: get rid of the cap value; N44 CLOSED, the lever stays dormant for NCAA Saturday**) · **`SNT_MAX_SPREAD_TICKS=40`** (trade Active/Defensive books; E32's gate of 8 is a latent silent-failure — measured across all 180 books 15-08: median 4 ticks, p90 6, **max 7**, so ONE tick of headroom before a widening book goes silent with no alarm) · **`SNT_INTERVAL_OVERNIGHT_S=20`** (the 174 non-game books, 6.7 min → 20 s — 🔴 **George has not named this number**; at 20 s the portfolio costs ~$30k/hr in spread, overwhelmingly paid to our own maker). ⚠ **INVENTORY IS THE REAL CEILING AND NO ENV ROW LIFTS IT:** the venue rejects a side-2 sell over `Pos − livS` as a WHOLE order, so a book that runs its holding down goes buy-only and then quiet whatever the config says — live 15-08, IPTCBEAR hit 293 shares and IPTCPANT 36. Raising it needs a float re-seed (E39, now sizeable from venue-true rebase numbers) or shorts ON (`SNT_SHORTS`, gated on E26 + T16). **The more active the taker is, the faster a thin book exhausts itself** | `inplay-market-maker` #44 (merged `0b9f601`) | ✅ on main | 🔴 not deployed — the env rows land at the next taker cutover | George: the OVERNIGHT number, then the cutover |
| 🔴🔴 **THE 33-HOUR HALT — the taker has been HALTED since 16-08 08:17:13Z (T-S05, IPTCHOUC venue=4467 ours=4439) and NOTHING ALERTED.** Root cause proven from the FIX wire: **the gateway DROPPED a real 28-share fill** (`MMSN2dec5d3406f347`, buy 28 @ 56.97 — wire shows 35=D, ack, FILL `32=28`; the taker journal has no such fill; 4439 + 28 = 4467 exactly). Mechanism: `oe_adapter.go:474` returns early on any exec report for an order with a request in flight unless it is that request's ack — the KNOWN open defect from the 15-08 TTL session. Precondition: gateway inbound lag **17–27 s** behind the venue at the incident, so the 1.5 s IOC cancel was in flight for effectively every fill. Chain: `SNT_INTERVAL_OVERNIGHT_S` 400→40 (15-08 20:08) → ~10× gateway traffic → lag → drop path routine → lost fills → divergence → correct halt → no alert. ⚠ HOUC is the FIRST book caught; others likely carry unrecorded fills — recover with a FRESH journal so the boot rebase re-bases every book. **Handover + full diagnostics: sessions/2026-08-17-handover-taker-recovery-and-fixes · sessions/2026-08-17-taker-33-hour-halt-diagnostics** | gateway `inplay-fix-gateway-go` (`oe_adapter.go:474`, no PR) · MM `src/snt/` (alerting, per-book halt, CANCEL STUCK flood fix, bounded auto-rebase — none built) | 🔴 nothing built | 🔴 **TAKER STILL HALTED** — recovery ceremony in the handover §1 (fresh journal, CFG-0026, OVERNIGHT back to 120 s) | George's go for the recovery; Hasan's review for the Go fix; Edwin for the T-S05 change (§3.4) |
| 🔴 **THE WEDGE — a lost cancel silences one side of a book FOR EVER (found live 15-08, PANT + BILL).** The IOC substitute sends ONE cancel 1.5 s after the order and `cancels_due` latches `cancel_sent`; `settle()` removes the order only on a TERMINAL venue event. So a cancel that is lost or never answered leaves the order in the LIVE map indefinitely — and the **wash guard** reads that map, so one wedged sell skips **every BUY on that book for ever**. The book then trades one side only, drains its inventory, and goes fully silent when the sell gate runs out. **Live evidence: IPTCPANT wedged 18:48 (sell 36 @ 55.28, market ~54), holding 4,133 → 36, ZERO fills for 14+ min and DEADLOCKED (buys blocked, nothing left to sell); IPTCBILL wedged 19:03, buys went to exactly 0 the same minute, dead 9 min, recovered on its own 19:19.** Across the run 6% of live-book minutes had a side missing, 2% fully silent — all of it PANT (37 min) + BILL (14). ⚠ **The rate exposed it: 40,573 sends in 2 h instead of ~7,700, so a rare lost cancel is now routine.** ⚠ **No operator lever exists** — `_manual_cancel` refuses twice over (manual-only EC12, and `cancel_already_sent`); only a taker restart clears it. **THE FIX (built, not pushed):** (1) `cancels_due` **RE-ARMS** every `cancel_retry_after_s` 🟡 2.0 s while the order is live, with `cancel_attempts` and a once-per-order `CANCEL STUCK` alarm at 🟡 5 attempts — the venue side actually clears; (2) the wash guard gets its OWN age-bounded view (`wash_fix_qty`, horizon = IOC window + one retry = 3.5 s) because it is an OPTIMISATION, not a rail — the 13-08 ruling kept the venue wash check ON and called the reject "the right outcome", so over-blocking (a wedged book for ever) is far worse than under-blocking (one venue reject). ⚠ **The SELL GATE keeps the unbounded `live_fix_qty`** — R-V07 rejects an over-size sell as a WHOLE order, so that number stays conservative; a test pins the two apart. ➕ Same branch, **Edwin 15-08: `daily_loss_budget_per_team` is env-tunable and 0 DISABLES the governor** (`SNT_LOSS_BUDGET`) — "remove the loss restrictions for simulation, we can experiment". Measured when he asked: busiest of 180 books had spent **$5,811 of $100,000 (5.8%)**, so it binds nowhere today; this removes the ceiling from rate experiments. T-M01 becomes "off for simulation, Edwin's ruling"; T-M03 and the inventory cap are untouched | `inplay-market-maker` **[#44](https://github.com/Novosapien/inplay-market-maker/pull/44)** — ✅ **MERGED to main `0b9f601`** (George's go, 15-08) | ✅ **1,236 tests**, ruff + mypy-strict clean, ON MAIN | 🔴 **not deployed** — the running taker (SNT-CFG-0023) still has the one-shot cancel | George: PR it or park it. ⚠ **PANT stays dead until the taker restarts** — the restart is the only lever that clears a live wedge (halt → stop → CFG-0024 + fresh journal → floats from the RUNNING env + snt20 drift → start). Session: sessions/2026-08-15-taker-rate-verification |
| ⚠ **NEW 15-08, found verifying #40: a book can spend its whole inventory BEFORE the inventory control ever engages.** The soft cap (1,500 sh, 80% flatten bias) measures `pos` — DRIFT from the float — while the sell gate measures `holding` = float + pos. On **IPTCBEAR** the 17:03 boot rebase adopted the venue's real position, **float 1,056** (journal said 3,372, −2,316), so holding reaches zero at pos −1,056 — **inside the ±1,500 cap, which therefore never fires on that book**. At 18:2xZ BEAR sat at pos −763 → **holding 293 sh**, i.e. ~7 median clips from sell-blocked. Below the 5-share minimum the sell gate goes quiet and the book becomes **buy-only** — directional flow on a bot whose whole purpose is uninformative flow (T-F02), self-correcting (buys rebuild the holding) but visible while it lasts. Not yet materialised: BEAR's per-10-min buy share is still 45–55%, indistinguishable from BILL's. **This is a consequence of the rate**: drift now reaches ±1,000 in about an hour where at 5.3 s it took a day. Exposure today: **1 book of 180 under the cap** (float min 1,056 · p10 3,127 · median 4,782), but **11 books are under 2,500** and every game night compounds the drift. Options (all Edwin/George, none built): cap drift at `min(1500, k × float)` · let the tilt/flatten bias key on `holding` not `pos` · re-seed thin books (E39's size question, now with venue-true numbers) · enable shorts on thin books (E26/T16, off today). ⚠ Also worth knowing: with shorts OFF, `max_short_shares` cannot help — the book simply stops selling | `inplay-market-maker` — no PR | 🔴 not built (finding only) | — | **George + Edwin (rides E39, touches T-M05/T-O08).** Watch BEAR's holding on the next game night; if it prints buy-only for a stretch, that is this |
| ⭐ **F5 REAL REVIEW — DONE 15-08 ~16:20Z (Q1), verdict FAIL for the train as it stands.** ✎ **SUPERSEDED BY EVENTS ~17:30Z: the operating session fixed every BLOCKER/HIGH/MED per review-002 and merged the whole stack to main (`1e0d265`: #31 #32 #39 #34+#37 #33-v2 #35 N45-fix PR #41 #38-dark) — see its own rows/notes for the fix verification.** Original verdict record kept below for the why. Seven independent adversarial reviewers, one per PR against its stack base, every finding verified by probe; nothing in any worktree modified. Full record: `specs/2026-08-14-mm-python-fix-set/reviews/review-002-f5-merge-train.md`. **Verdicts:** #31 MERGE · **#32 BLOCK** (`_ANCHOR_TYPES` omits `ANCHOR_SEED` → the seeded anchor is invisible to the NEXT run's tail fold → the chain breaks at the 2nd consecutive cutover and journals the WRONG anchor as fact — the 14-08 incident's own shape; one-line fix, verified across 5 cutovers) · #34 MERGE-WITH-FIXES (**HIGH:** `_ACKED_AT_VENUE` still nets PENDING_REPLACE/CANCEL after the venue removed them → sends through a real bid, the $50,366 shape at the other end of HIGH-1's round trip; + opening ladder unjudged, STALLED alarm mis-diagnoses, CONVERGE_STALE latches off) · #37 MERGE-WITH-FIXES (one `examined` counter shared LIVE+REST → ≥64 refusing LIVE books starve ALL of REST, regression vs base; the 30 s stall bound is now ~84 s; AC9 wording false; refused book one-sided for the whole episode = a 2nd unrecorded R-Q01 yield) · #38 MERGE-WITH-FIXES (**HIGH:** the bound does not survive the reconciler — rest-until-gone keeps the oversized rung, settled 27,000 vs holding 18,000, R-V07 rejects; must fix BEFORE N43 supplies a number) · #33 MERGE-WITH-FIXES (**HIGH:** the "60 s pre-roll" is a +60 s stamp skew on EVERY reading, suppressing ~45% of ack load — every absolute baseline and profile JSON measured under it; headline unchanged, numbers need re-cut or caveat) · #39 MERGE (differential fuzzer old-vs-new, 3×200×400 incl. dups/out-of-order/orphans/checkpoint round trips: ZERO divergence) · #35 MERGE for lineage (+ an UNWIRED docstring marker). ⚠ **B2 — a stack-level BLOCKER whose owner is MAIN, not any PR: `pending_exposure` (un-journalled venue intent) feeds EPR → checkpointed pre-variation sizes → live and replay DIVERGE; masked on the 500 grid, constant since #36's grid 1. Verified twice (reviewer + lead: fails on `main@2c74886`, passes on `ed921ca`). #38's AC9 test goes red on merge. Needs George's ruling → N45; recommendation: EP's pending term counts only acked states.** Deploy coupling: #34 never without #37; #38 never activated before its HIGH. Merge order once fixed: #31 → #32 → #34 → #37 → #38 → #33 → #39 → #35 | `inplay-market-maker` #31–#39 | ✅ reviewed | 🔴 nothing merged from the stack yet (freeze stands regardless) | **George:** the N45 ruling (B2) + a fix session for B1/HIGHs; then the drill (the mechanical gate on top, never instead) |
| ⭐ **THE PRUNE FIX IS THE CURE — rig-verified 15-08.** On the production VM shape (n2-standard-2), provenance-asserted adjacent arms, six-game workload at 1×: **miss ratio 28.76% → 0.00%**, ms/ack p50 **7.346 → 0.298** (24.6×), and the engine completed **2× the decision cycles** in the same wall clock. The ~35% missed sweeps that drove the panel flapping and the DEFENSIVE capping were ONE O(all-orders) scan with a datetime parse per retained dead order, on every venue event. ⚠ Caveats: DRAIN_CAPPED is 6/7 on BOTH arms (the boot re-stand hitting the 512 cap — not game load; AC4's zero-clause needs a GATE ruling), and the arms ran on workload **v1** (the +60 s stamp skew ⇒ ~45% under-loaded; both arms share it so the comparison is internally valid, but the GATE re-measures on v2). Evidence: `specs/2026-08-14-mm-python-fix-set/profile-cb4-verdict.md` | `inplay-market-maker` — **MERGED to `main@23602ae`** with the whole fix set | ✅ merged, 1,227 tests green | 🔴 not deployed (freeze) | George's go |
| **THE FIX-SET BUILD (`specs/2026-08-14-mm-python-fix-set`)** — Phase 0 landed: the dictionary batch (6 rows: `prior_run_dir`/`MM_PRIOR_RUN_DIR` · `tob_stale_after_s` 30 s · `MM_MARKETABLE_GUARD` on · `MM_BOOT_HEAL` on · `live_phase_offset_buckets` 8 · `opening_position_shares` 0/E27) + R11 in the deploy runbook. Phases 1–4 (ANCHOR_SEED · profile/workload · R-Q09 guard · de-phase · R-Q08 cap · incremental sweep · healer · gate) run as 2 streams + lead per the spec | `inplay-market-maker` **#31** (base `feat/always-quoting-step4b@5b10d68`) | ✅ built — 885 tests, ruff + mypy clean | 🔴 not deployed (defaults only, no consumers yet) | F5 real-review pass before merge (Q1); NOTHING deploys without George's explicit go; R11 gates timing |
| **FIX-SET CA1 (F2) — `ANCHOR_SEED`: kickoff anchors survive a fresh journal.** The composition reads the PRIOR run's directory once at boot (`MM_PRIOR_RUN_DIR` → the `prior_run_dir` dictionary slot) and journals ONE seed FIRST; after that the anchors are a journalled fact and replay reproduces them without reading the prior run again. Lenient reader (checkpoint integrity-hash only + journal tail folded through the real rules), **NOT `load_latest`** — R-D06's per-deploy config bump would have made that silently empty for ever (review H1). Ends the p_ref rebase defect two rows below ($0.685/share on BENG; up to $5 in the regular season). **✅ review-f2 hardening landed 14-08 (`4a7c484`)**: a hash-valid prior run whose VALUES would not parse (`status: "in_play"`, `x: "not-a-number"`, a nested `x`) used to stop the maker BOOTING — every candidate game is now proved by constructing the real `GameBelief` on the read side, and `decimal.InvalidOperation` (an `ArithmeticError`, NOT a `ValueError`) is caught in both fold guards. A typo'd `MM_PRIOR_RUN_DIR` no longer mints an empty seed that permanently burns the one re-mint — it logs `FAILED`, journals nothing, and stays re-mintable | `inplay-market-maker` **#32** (base `phase0/fix-set-dictionary-batch`, stacked on #31) | ✅ built — 932 tests (885 + 47), ruff + mypy clean | 🔴 not deployed | ✅ ops surface closed 14-08 (lead commit `6ec78fa` on the PR: `__main__` env table + the runbook's standing cutover step "set `MM_PRIOR_RUN_DIR` on every cutover"). Then F5 real review + the drill; George's explicit go; R11 gates timing |
| **FIX-SET CA2 (F3/R-Q09) — the pre-flight marketable guard at the converger.** The engine prices from its own valuation and never from the book, so its bids cross stale third-party asks on every repost — live 08-09 a COWB bid at 76.04 swept 8 levels, 920 shares, **$50,366**, while intending to rest. The converger now asks ONE question per book between the reconciler's diff and the FIRST register: does any submit or replace price at or through the live opposite touch, **net of our own resting quantity**? Yes holds the WHOLE batch back — nothing registered, nothing sent, target left staged for the next pass (equal price counts as through). The placement is forced, not chosen: a partial register would leave the Venue State Record holding intent for a batch that never went (`[register-then-send]`), and a partial send collides ClOrdIDs minted BY POSITION plus leaves one-sided exposure (`[atomic-book]`). Netting is review H5's requirement — the venue book is ANONYMOUS, so judging against the raw touch would refuse us against our own stale ask, which is what the machine does all day; a pending replace counts at its OLD price, never its destination. **Fail-open throughout** (no book · stale > `tob_stale_after_s` 🟡 30 s · unparseable · a side entirely ours → SEND) because refusing on absent data would silence quoting. `MARKETABLE_REFUSED` names the price AND the touch that refused it, rate-limited (first, then every 50th, with `repeats=`) — the 08-13 fire loop proved a log flood starves the heartbeat. Tick line carries `refused=`. **AC9 by construction:** the guard reads a live feed + a clock so it acts at the venue EDGE only; nothing it holds reaches `Orchestrator.state()` or a checkpoint, and replay never re-drives `converge()` ✎ **FOLLOW-UP LANDED same PR (lead-directed):** `MARKETABLE_GUARD_STALLED` — after `marketable_stall_passes` consecutive refusals of the SAME book the guard alarms **once per episode**, naming the book, the touch holding it, and the heal in priority order. Bound **DERIVED: 120 passes ≈ 30 s** (converger runs its own task at `converge_interval_s` 0.25 s; a refusal costs no budget so a LIVE book is re-judged every pass) — a test pins `passes × cadence == 30.0` so drift fails the build. One-row dictionary exception, lead-approved, 🟡 OURS, `parameters.md` row added. An alarm, **not a mode** — sibling of `converge_staleness_alarm_s`; it changes nothing about what is sent ✎ **PHASE REVIEW BACK 14-08 — 1 probe-confirmed HIGH, fixed same PR.** **HIGH-1: the netting counted `PENDING_SUBMIT`**, i.e. orders we had published but the venue had NOT acked, so they were not in the book being netted against. That state exists ~14–264 ms EVERY converge pass (register → ack → MD propagation), and in it the guard subtracted quantity from a level it did not occupy, netted the level to zero, stepped past a REAL external touch and sent — the reviewer's probe put a sell at 75.50 into a live external 76.00 bid, **the $50,366 shape in the exact scenario R-Q09 exists for**. Now only venue-ACKED states net (ACTIVE/PARTIALLY_FILLED/PENDING_REPLACE/PENDING_CANCEL). ⚠ **Lesson: §4.4's `_EXPOSURE_STATES` is NOT a book-presence set** — it answers "could this cost us money" (where PENDING_SUBMIT/UNKNOWN belong); the guard asks "is this IN the published book" (where they do not). Also fixed: **MED-1** the guard could kill the whole converge task (two probes: `data` as a LIST, and a single-level NaN price that slips the parse because sorting ONE level never compares it) → `refuses()` is now a boundary, escapes are counted and SENT; **MED-2** a blind guard looked identical to a quiet one → `MARKETABLE_GUARD_BLIND` once, plus tick-line `books=` (a LIVE gauge — a dead feed freezes cumulative counters wherever they stopped); **LOW-1/2/3**. Scope recorded: the guard's claim is **external-only**, self-cross tolerated per N12 `[post-first]` | `inplay-market-maker` **#34** (base `fix-set/ca1-anchor-seed`, stacked on #32 → #31) | ✅ built — **986 tests** (932 + 54), ruff + mypy clean, replay equality green | 🔴 not deployed | ⚠ **The phantom-book risk is live and untested:** the depth feed's fresh-but-PHANTOM mode (08-08, a JETS ask at 45.44 shown ~5 min against a journal-confirmed unfilled bid at 45.45) makes the guard refuse against a touch that does not exist, and a refusal keeps the target STAGED — that book stops converging until the phantom clears. **Heal with `POST /md/book-resubscribe` FIRST** (fixes the feed), `MM_MARKETABLE_GUARD=off` second (blinds the guard; off is total — no cache, no subscription, no cost). Fresh-but-EMPTY (10-08) is harmless — reads as no opinion. ✎ **DETECTION now closed** — `MARKETABLE_GUARD_STALLED` fires ~30 s into a stall and names the heal, so this is one alarm to read, not a counter to watch. **The AUTOMATIC exit is still open (N41)**: the alarm changes nothing about what is sent, so a phantom nobody answers stalls that book indefinitely — George's call. ✅ **MED-3 + MED-4 BOTH BUILT 15-08 in [MM #37](https://github.com/Novosapien/inplay-market-maker/pull/37)** (stacked on #34) — see the row below. Then F5 real review; George's explicit go; R11 gates timing |
| **FIX-SET CA2b (MED-3 + MED-4) — the refusal path finished.** **George's ruling (MED-3): a refused book STILL SENDS ITS CANCELS** — submits and replaces wait, cancels go. The case that forced it is a FALLING market: our resting bid is stale and due for cancel while its replacement prices through the live ask, so the whole-batch rule left the stale bid resting — *the order most likely to be hit* — because the guard disliked its successor. **Risk reduction must never wait on risk addition.** The subset is safe on three properties that already existed: exposure only ever SHRINKS during a refusal (the half-posted-ladder half of `[atomic-book]` is about ADDING one side, so unreachable) · no submit ids are minted, and a cancel's id mints from the ORIGINAL order's id (`CXL-{client_order_id}`) never BY POSITION, so it is stable across re-diffs (the ClOrdID-collision half unreachable) · `register_cancel` → PENDING_CANCEL while `_ACTIONABLE` is {ACTIVE, PARTIALLY_FILLED}, so the re-diff ignores an order already leaving and **the cancels go exactly once**. ✎ AMENDS the 14-08f line "a refusal costs no budget" — cancels are real messages and count. **MED-4: a pass is now bounded TWICE, by instructions SENT and books EXAMINED** — the instruction budget cannot bound a refused book (the guard answers AFTER the diff, so it costs a whole `reconcile_book` and spends no budget; a phantom touch re-diffs the universe every pass for ever). 🟡 `converge_max_books_examined_per_pass` **64**, DERIVED (a pass can only SERVE ~10 books at 128 instructions, so 64 is >6× the productive work and cannot bite on healthy traffic); **budgeted passes only** — the unbudgeted `[compat]` flush (boot, pull path, direct-drive tests) still converges everything. ⚠ **The cap CREATES starvation without the fairness fix, so both landed together**: the rotation advanced only on SERVED books, so persistent refusers at the head would be re-examined every pass, hit the cap, break, and the books behind them would never be reached AT ALL. Being refused now counts as progress through the rotation. ⚠ **Gap the review did NOT name, found by the tests: the LIVE class had NO rotation at all** — it never needed one because a SERVED book leaves the dirty set and a static order drained itself, but a REFUSED book stays. LIVE now rotates on its own cursor. **General lesson: a work list that drains itself needs no fairness mechanism; the moment an item can stay on the list without being completed, it does.** Both starvation tests verified by MUTATION (revert the cursor increment → both fail) ✎ **PHASE-3 REVIEW 15-08 — a reproduced HIGH in the cap itself, fixed same PR: it starved the converger through SUSPENDS**, the one class with no cursor. Suspends run FIRST, re-stage every `BookSuspended` cycle, and a suspend with nothing left to cancel yields ZERO instructions — so ~100 suspended books spent the whole cap at the same alphabetical break point every pass and **no live book ever converged** (not late — never). **A suspend is now NEVER charged and can never break the pass.** A cursor cannot fix this class (rotating risk reduction spreads delay, does not remove it); charging only instruction-YIELDING targets was rejected because the bounded cost is the DIFF, already paid before the yield is known. Exemption stands on its own terms: risk reduction is never rationed here, and a suspend is SELF-DRAINING. ⚠ **Third appearance of the same shape in one chunk** (rest had a rotation · live did not · suspends cannot) — the lesson was already in decisions.md when the bug was written, which is the process note. ⚠ Recorded NOT fixed: a zero-instruction suspend still costs a `_suppression()` call = a scan of the WHOLE portfolio reject table (`backoff.py:113`) — on the per-event-scan list. Plus LOW-2 (frozenset membership on the 4 Hz walk) and LOW-4. Reviewer verified clean: cancels-exactly-once, CXL-id stability, compat-flush exemption; refusal-cancel budget exhaustion judged self-limiting | `inplay-market-maker` **#37** (base `fix-set/ca2-marketable-guard`, stacked on #34) | ✅ built — **997 tests** (986 + 11), ruff + mypy clean, replay equality green | 🔴 not deployed | Merge order is the lead's: **#34 first, then #37**. Review pass on #37; George's explicit go; R11 gates timing. 🟡 Second one-row dictionary addition — flagged to the lead (a module constant instead is a one-line change) |
| **FIX-SET CA3 (F3/R-Q08) — the ask cap: RESIZE, not reject.** R-V07 (live probe 08-09) refuses a sell over `Pos − livS` as a **WHOLE order** — never a part-fill, never a short — so an unbounded ask ladder does not lose one rung when it breaches, it loses the ENTIRE ask side of that book. The ladder is now sized into `capacity = holding − livS` at the one place a Target Order Book is constructed: levels paid inside-out (the touch keeps its shares, per the touch-heavy profile), each capped quantity FLOORED to the grid, and a level that cannot reach `min_quantity` DROPPED — dropped rather than shrunk to the increment because §5.10's `quantities_within_bounds` rejects a sub-minimum level and a failed check raises, blocking the whole book, which is the one outcome R-Q08 exists to prevent (✎ corrects the chunk brief's "drop below 500"). The grid is a PARAMETER defaulting to the dictionary row, so the cap follows the 15-08 `qty_increment` 500 → 1 ruling (#36) instead of pinning a grid. ⚠ **livS EXCLUDES `ACTIVE`/`PARTIALLY_FILLED` and the derivation is the reconciler's own reach** — `_ACTIONABLE` is exactly those two, so the target ladder ALREADY accounts for them (rest-until-gone keeps a still-wanted rung, a moved price replaces it); counting them as well double-counts the ask side against itself and a fully-offered book would empty, watch the cancels drop livS to zero, re-offer, and **oscillate at the 500 ms pulse across every book**. livS counts `PENDING_SUBMIT`/`PENDING_REPLACE`/`PENDING_CANCEL`/`UNKNOWN` — everything the pass cannot touch — a replace at **max(old,new)**, for which `pending_quantity` was added to the Venue State Record (only the registration knows the new; cleared everywhere the replace ends; an older checkpoint restores None and falls back to the old quantity). ⚠ **NOT §4.4's `_EXPOSURE_STATES`** — the review-ca2 lesson in a second place, and a test pins the two sets apart. **A capacity ≤ 0 empties the ask side, bids untouched** — a documented one-sided state where **R-Q01 yields to R-V07** — with `ASK_CAP_NEGATIVE` once per episode and `ASK_CAP_RECOVERED` on the way out (edge-triggered; 180 books at 2 Hz cannot afford a per-cycle line). **AC9 by construction:** the bound is built partly from venue INTENT and replay never re-drives `converge()`, so a replayed run computes a DIFFERENT bound — it cannot matter, because the cap lands AFTER §5.8's decision, AFTER every §5.7.3 draw and AFTER the reshape, on final quantities only, and `_BookRecord` stores prices, PRE-variation sizes and the IA. A test drives the live arm with an 8,000-share in-flight sell the replay arm never sees and both reach byte-identical core state. ⚠ Standing constraint recorded in code: if TOB quantities ever enter checkpointed state, the argument dies and the cap moves to the edge | `inplay-market-maker` **[#38](https://github.com/Novosapien/inplay-market-maker/pull/38)** (base `fix-set/ca2-marketable-guard`, stacked on #34) | ✅ built — **1,056 tests** (986 + 70), ruff + mypy clean; replay equality green (drill: deterministic core byte-identical, settled venue book value-identical) | 🔴 not deployed — **and INERT if it were** | 🔴 **N43 — George's ruling, and it is a DEPLOY INPUT not a code question.** `opening_position_shares` is 0 (E27) and 0 reads as UNKNOWN, so the bound **fails open** and says so once at boot (`ASK_CAP_UNBOUNDED`). Enforced at the stub it takes **every book bid-only on the first tick after a cutover** — R-V07's `Pos` is the VENUE's position while our journal starts at 0, and the two are known to differ (14-08: IPTCJETS sold to net −197 from a zero journal position because the ACCOUNT holds seeded inventory). Proven, not argued: wiring it unconditionally turned three existing tests red on a missing ask side. Fail-open is the direction 14-08f already chose for R-Q09 on absent data. **Two ways to close it: supply the account's real seeded inventory as the opening position (one number, and the whole mechanism turns on), or rule that fail-open stands until E27.** ⚠ Sizing is only HALF of R-V07 — the venue applies the rule per order at SUBMIT time, so the converger must also never land new sells on top of old ones it is about to cancel (`venue/sync.py`, frozen for this chunk). Then F5 real review; George's explicit go; R11 gates timing |
| **FIX-SET CA4 (F4/R8) — the boot healer: cancel-unknowns, maker-only. R-D05 CLOSED, and R-D06's fresh-journal ceremony RETIRED for the maker.** At boot, after the replay and BEFORE the book stands, the engine reads the gateway's own MM order index (`GET /orders/mm` — its PR #5, live in `main@a41e540` since 02:49Z today, which CORRECTS the chunk brief's "not yet deployed") and diffs it against the Venue State Record. **Ownership is the ClOrdID scheme alone**: `MM` + 16 lowercase hex is a SHA-256 tail, the taker's `MMSN` + 14 hex fails the hex test on its SECOND character — both are 18 chars and both start `MM`, so the boundary reads thin until written down, and a test feeds the taker's own `mint_id` to the classifier rather than a copy of the scheme. Ours + unknown → CANCEL, one loud line each · `MMSN…` → never touched · `MM`-prefixed but not our scheme → **LEFT resting + ALARM** (on doubt, never touch a resting order) · anything else → never touched · an unreadable entry → skipped + ALARM. ⚠ **The universe does NOT narrow ownership** — an own-scheme order on a book this run does not quote is still ours and still unmanaged, so it comes down with its symbol named (exactly what the old ceremony's global `cancel_all` did to it). ⚠ **"Known to the record" = the NON-TERMINAL set (`open_orders`), chosen explicitly — the exposure-set lesson for the THIRD time in one build.** Not §4.4's `_EXPOSURE_STATES` (a money question), not `_ACTIONABLE` (a second cancel for an order already leaving), and NOT "any id the record holds": a TERMINAL-in-record order the venue still shows OPEN would then rest for ever, since the reconciler only ever sees `open_orders` and the record drops the row at the 300 s retention window. ✅ **DEVIATION from the spec's wording, flagged to the lead BEFORE building and APPROVED BY THE LEAD 15-08 AS THE DESIGN ("strictly better than the spec's local retire"; the lead carries the ✎ into spec.md): "known-but-absent → retire locally" is built as a CANCEL.** A local retire writes boot-time state out of an HTTP snapshot that replay has no way to reproduce, so AC9's settled-book claim breaks on the next drill; and the index is a SNAPSHOT whose own route documents that the caller owns staleness, so an order submitted microseconds before the call can be missing from it — retiring on that evidence would forget a REAL resting order and repost the level (the 07-08 doubled-levels defect). The cancel gets the truth back either way: it takes down a real order, or it draws `ORDER DEAD`/`UNKNOWN ORDER` and the journalled `[gone-retire]` path retires it. **So the healer writes NO engine state at all**, and a test pins it — canonical orchestrator state AND journal length unchanged across a heal that cancels in both directions (mutation-checked: a healer that retires locally fails it). **Fails open at every step** — flag off · URL unset · route absent (a gateway rolled back past PR #5 answers 404) · 401 · 503 · refused · timeout · non-JSON · wrong shape · a body past the 16 MB cap — each with its own reason and today's behaviour; a dead transport mid-heal is caught too. ⚠ **The operational corollary that will bite a half-applied cutover: the journal and the config version now MOVE TOGETHER.** Keep the journal → keep the version (`load_latest` only accepts a checkpoint from the RUNNING version, so a bump rejects every checkpoint and replays a journal that now grows across deploys); take a fresh journal → bump the version (a fresh journal restarts each book's quote-version counter, so the reconciler re-mints ClOrdIDs the venue remembers — the 07-08 duplicate-reject deadlock). Both shapes are now a table in `deploy/OBSERVABILITY-REDEPLOY.md` §2.2, with the fresh-journal shape kept as the healer's documented rollback. Env, all new and all deployment facts (no dictionary rows): `MM_GATEWAY_OPS_URL` (unset = no heal, loudly) · `MM_GATEWAY_OPS_KEY` (a SECRET, `repr=False` on Settings) · `MM_BOOT_HEAL_TIMEOUT_S` 🟡 5.0 s (a sixth of the 30 s boot grace). Decisions 2026-08-15f · session sessions/2026-08-15-ca4-boot-healer | `inplay-market-maker` **[#42](https://github.com/Novosapien/inplay-market-maker/pull/42)** (base `fix-set/ca3-ask-cap`, stacked on #38 → #34) | ✅ built — **1,119 tests** (1,065 + 54), ruff + mypy clean. Seed §R8 (a)–(g) all asserted at the VENUE INSTRUCTION STREAM per the `[seed-silent]` lesson, the zero-heal case included (it asserts NOTHING was published). ✎ **HARDENED 15-08 per review-ca4 — 2 HIGH, 3 MED, 2 LOW, all probe-verified, all now tests.** HIGH-1: a 200 with NO `orders` key read as "the venue is empty" and would have planned a WHOLE-BOOK cancel — absent is now a shape error (no heal), only an explicit null is empty, and the route's own `count` is cross-checked. HIGH-2: urllib's `timeout=` is per-recv, so a trickling gateway ran **19.09 s at timeout=0.5** on the event loop inside the 30 s grace — the read is now bounded by a wall deadline between chunks AND runs off the loop under `wait_for`, which makes the runbook's "a sixth of the boot grace" true rather than aspirational. MED-1 3xx refused (a 302 had walked the X-Ops-Key to another path) · MED-2 a mid-heal publish failure reports **PARTIAL** with sent-of-planned instead of "today's behaviour" · MED-3 NaN timeouts refused by name · LOW-1 duplicate index rows cancelled once · LOW-2 runbook samples marked illustrative. ⭐ Found by the suite, not the review: `build_opener()`'s default `ProxyHandler` calls `getproxies()`, which on macOS leaves the process fork-hostile — and this engine FORKS for every §10.3 checkpoint — so the opener carries an EMPTY ProxyHandler, which also stops an `http_proxy` routing an ops-key'd request through a third party | 🔴 not deployed (the 14-08 freeze) | ⚠ **The GATE owns the other half of AC8**: the rig drill with planted orders, the season-scale (≥1 GB) checkpoint-bounded boot timing, and ONE real-VM cutover WITHOUT a fresh journal (boot clean, book stands, taker unaffected, no reconcile halts in 30 min). ⚠ **A number for that drill to read:** the heal's RETURN traffic is one venue event per cancel, and a boot after a dead-man sweep can cancel the whole record (~1,660 orders today) — those answers land against the 512-per-tick drain cap, which at CB1's pre-CB4 9.893 ms/ack is ~5 s in one tick, i.e. the beat-stall threshold. Read the heal's own numbers off the boot log and confirm the beat holds. Then F5 real review; George's explicit go; R11 gates timing |
| ✎ **CORRECTION — `market.book.*` IS published; the standing vault line was stale.** Direct-verified on the VM 14-08 as CA2's spec-mandated step 1: the deployed gateway runs `TZERO_MD_FULL_BOOK=true` + `TZERO_MD_BOOK_SYMBOLS=*` + `TZERO_MD_BOOK_REPUBLISH_SEC=5`, so **every** symbol publishes on change plus a 5 s republish of each non-empty book. The old "defined but never published, do not build against it" line described the pre-08-08 world — struck through in `decisions.md` and `build/venue.md`. The taker has been gating every live order on this feed for days; R-Q09's guard reads the same one. **Subscribe one subject per symbol, NEVER `market.book.*`** — a NATS wildcard matches ONE token and the twins carry a dot (`IPTCRAVE.TEST`), so a wildcard silently misses them | — (a fact, not a change) | ✅ recorded | — | — |
| ⭐ **FIX-SET CB1 (F1a) — tick-time instrumentation + THE SIX-GAME WORKLOAD + the profile.** Per-stage timers as a side-car on the runtime (`TickTimers`/`NO_TIMERS`; clock is `perf_counter`, never the injected `now`, and nothing measured reaches an engine, envelope, `TickReport` or checkpoint — replay equality holds by construction). `scripts/six_game_workload.py` builds `six-game-v1` (seed 20260814, six Chiefs–Ravens copies under fresh `9999…` ids, Z-stamped, 12 securities, 170 books) — **the fixed baseline AC2 and AC4 are measured against**; every run writes a manifest so a later chunk can prove it ran the same thing. **THE FINDING: the venue ack drain is 98.1% of tick time at 1× (p50 1,162 ms of 1,187 ms); the sweep everyone planned to optimise is 1.8%.** ms-per-ack is **superlinear in book depth** (1.455 → 6.313 → 9.893 ms as mean acks/tick goes shallow → 48.8 → 129.1), so every fixed-ms-per-ack capacity projection in the vault is optimistic. **AC1 PASSED**: instrumentation adds **4.240 µs/tick** (0.00085% of the 500 ms interval). ✅ **R1's sanity check RESOLVED by the 3-game validation arm** (lead-authorized, 20:53→21:28Z): at production's own game count the venue drain is still **97.0%** of tick time and the miss ratio is **23.5% vs the live ~35%** — the finding is not a rig-overload artefact. ⚠ Two corrections fell out of it. (1) The bar is widely mis-quoted as "~35% **late ticks**"; every vault source says **missed sweeps on ~35% of ticks**. (2) **The per-ack gap was a comparison against a number nobody ever measured.** `decisions.md`'s 0.17 ms/event is real but its own caveat **excludes inbound ack processing**; the ~0.5–1 ms/ack in `drafts/always-quoting-step4-design.md` is an **ESTIMATE**. CB1's 8.5–9.9 ms/ack includes exactly what the 0.17 excluded, so **CB1 contradicts no measurement** — it contradicts the estimate R1 sent it to test. 🔴 **Consequence: that design's Saturday arithmetic ("~1–2 s of every second on acks") becomes ~17.9 s of compute per second — anything built on the 0.5–1 ms estimate is optimistic by 10–18× and needs re-deriving.** ⭐ Also learned: halving the games cut acks only 12% — **book count drives the ack volume, not game count**. ⭐ **AC2 re-cut (review HIGH)**: p90 acks-per-TICK is confounded (= arrival rate × pass duration, so it rises when the loop slows) and CB2 could never move it; R2/AC2 amended to **p90 acks ARRIVING per 500 ms wall-clock window** — baseline **214** (six-game 1×, gate ≤107), tool `scripts/ac2_ack_windows.py`, no re-run needed (journals carry `receive_time`). Arrivals are spikes not clumps: **66% of windows empty, one burst per pass, 0.018 ms between acks inside a burst** — so de-phasing must be judged on burst SIZE, not spacing (🟡 projected p90 ≈73). Measured on `cb1-profile-clone` (**n2-standard-2** — R1 said e2-medium; prod is not, spec amended by the lead), Python 3.12.13 (prod patch unverified) | `inplay-market-maker` **#33** (base `phase0/fix-set-dictionary-batch`, stacked on #31) | ✅ built — 916 tests (885 + 31), ruff + mypy clean | 🔴 not deployed (measurement only; timers OFF by default, so production pays nothing) | **George's call on two 🔴 questions before CB2/CB3 are judged against AC4**: (1) CB3's incremental sweep targets 1.8% of the tick and cannot reach AC4 alone — CB1 recommends **re-ordering CB4 ahead of CB3**; (2) is CB4's one-session timebox right when AC4 needs a **>100×** improvement (52.3% miss ratio at 1× vs < 0.5% target)? Then F5 real review |
| **CB1's drain-cap ruling — 🟡 DO NOT RAISE THE VENUE CAP** (supersedes the standing "the cap must rise toward ~1,050 acks/tick since group commit" note in `build/runtime.md` `[drain-cap]` and the step-1 row below). The premise is confirmed — commit is 2.4 ms against a 1,162 ms drain, so engine time IS the binding constraint — and that is exactly why the cap cannot rise: at 9.893 ms/ack a 1,050 cap authorises a **10.4 s** drain, 2× past the 5 s beat-stall threshold, so the heartbeat would be withheld and the dead-man would sweep the book (the 08-13 fire-loop shape). The cap fires on only **4 of 1,747 ticks (0.23%)** — it is not the lever, the per-ack cost is. Rule to hold the dictionary to: `venue cap ≤ drain budget ÷ ms-per-ack`; a 1,050 cap needs **≤ 0.286 ms/ack, a 35× improvement**. The readings cap (256) has 23–43× headroom and never fired | `inplay-market-maker` **#33** (documented in `specs/2026-08-14-mm-python-fix-set/profile-cb1.md` §6) | ✅ **NO dictionary change proposed by CB1** | — | Re-measure and re-derive after CB4 cuts ms-per-ack |
| ⭐ **FIX-SET CB2 (F1b-dephase) — MEASURED, NOT WIRED: AC2's gate cannot be met by moving the pulse.** The chunk asked for a deterministic per-book phase offset across the 500 ms LIVE pulse so game-load republish waves stop landing on one edge. Built the primitive (`src/mm/quotes/phase.py` — security id → one of `live_phase_offset_buckets` 8 slots, deterministic on the id ALONE, deliberately NOT salted by config version so a book's slot survives a deploy, integer-microsecond absolute grid, gate is "has a new slot begun" not "am I inside my slot" so a slow caller degrades to today's behaviour instead of starving 7 books in 8). Then measured the premise before wiring it, and the premise does not hold. **(1) The metric is invariant under the mechanism** — AC2 counts acks per 500 ms window and the pulse is 500 ms, so a book redrawing once per pulse lands in exactly ONE window per pulse whatever its offset is; a phase shift moves WHICH window, never HOW MANY. Same class of defect as the acks-per-tick confound #33 already fixed. **(2) The books are already de-phased** — `_timer_due` measures 500 ms from each book's OWN last publish, not a shared grid, so LIVE books free-run; over 200 s, 150 burst-clusters held 2 books, 11 held 1, 2 held 4, **none held all 6**, and the only coincident pairs are the two sides of one game (they share a READING, not a pulse edge). An absolute 8-bucket grid would CONCENTRATE a currently-continuous distribution. **(3) The gate's own percentile is not game load** — LIVE books are 25% of acks (3 games live), the other 158 books 73% on their 5–40 s dwells; the mean window holds 49.8 acks across 4.2 books, and the p90 window contained **zero** live-book acks. **(4) The quiet books are already maximally spread** — 2.83 bursts/window, p90 = 5, and Poisson(2.83) predicts 5 exactly. **(5) The closing arithmetic:** redistribution never changes a total, so a perfectly flat arm has p90 = mean. Mac unsaturated: mean **43.3**, gate **41** — **the gate sits BELOW the mean arrival rate, so no scheduler can reach it**; perfect flattening of every redraw gives 47% and stops. Clone 1×: mean 46.9, gate 107 — reachable arithmetically, but the clone's p90/mean is 4.6 vs the Mac's 1.89 and that excess is **loop saturation** (1.375 s/pass, converger getting ~1.23 passes/tick), which a phase offset cannot unpick. **THE A/B SETTLES IT BY MEASUREMENT:** two 2,400 s arms, same seed, one binary; the ON arm released each LIVE book at its own slot from a converger at one bucket width. The mechanism ENGAGED — release clusters +27%, acks per release −23% (26.9→20.8), books per release −22% (2.36→1.84), single-book releases **4.3×** (385→1,648). **The gate did not move: p90 90 → 90**, p50 44→44, mean 46.39→46.30. Burst size fell a quarter and the metric registered nothing. **The only lever that moves this metric is sending FEWER acks, not sending them at different times — CB3 and CB4.** ⭐ **THE LEVER BOARD (reporting, for George):** the **158 quiet books produce 66.8%** of all acks (149,598 of 224,034; 12,820 redraws, mean 11.7 acks) on their 5–40 s dwells — the biggest ack-volume lever in the machine is **one dictionary row and it is BOOK-VISIBLE** (Edwin's remit, 22-07 line, George's 08-11b numbers, E31/E17 round). Modelled: dwell ×2 → mean −33.3%, p90 −26.7% (90→66); dwell ×4 → mean −50.0%, p90 −40.0% (90→54); quiet-books-alone floor p90 64. Both are UPPER bounds (a book that waits longer has moved further, so its real diff is larger). Even ×4 misses AC2's 50%. ⚠✎ **15-08: v1 numbers — the SHARE inverts on v2** (the feeder skew suppressed the GAME books' redraws, not the quiet books', so the restored ~45% lands on the live side: projected v2 ≈63% LIVE / 37% quiet vs v1's 33/67). Quiet-book ABSOLUTE volume unchanged; the dwell row stays a real lever, just not the biggest; the ×2/×4 p90 projections need re-deriving on v2. ⭐ **AC4 IS A MACHINE-SPEED RESULT, not an engine result:** same code, same workload, same 1× arm — the n2-standard-2 clone misses **52.3%** of due-sweep ticks, this Mac misses **0.12%** (OFF) / **0.11%** (ON), i.e. **4× INSIDE** AC4's <0.5% target at double production's game count. The 52.3% is per-ack cost against 2 Cascade Lake vCPUs. ⚠ AC4's zero-DRAIN_CAPPED clause fails on BOTH arms (7 capped = the boot re-stand hitting the 512 cap, not game load). Strongest datum yet for CB4-before-CB3 and for Go. | `inplay-market-maker` **#35** (base `fix-set/ca2-marketable-guard`, stacked on #34 → #32 → #31 — CB2-after-CA2 merge order resolved by stacking) | ✅ built — **1,009 tests** (986 + 23), ruff + mypy clean; the module is UNWIRED by decision, so replay equality holds by construction (no caller, no clock, no state, no event) | 🔴 not deployed (no consumers — nothing in the engine calls it) | 🔴 **George's call: withdraw AC2's ≤50% gate as written.** Either re-scope it to a window SHORTER than the pulse on the LIVE population (the only place the mechanism is visible), or fold F1b-dephase into CB3/CB4 and serve R2 by cutting ack volume — CB2 recommends the second, which is also what `profile-cb1.md` concluded (sweep side 1.8% of the tick, drain 98%). AC2 was George's "works out" proof for Q2, so it is his to withdraw |
| ~~**Gateway ops route `GET /orders/mm`**~~ ✅ **DEPLOYED 15-08 02:49Z as a passenger in the TTL binary** (`main@a41e540`) — see Landed | `inplay-fix-gateway-go` **#5** (merged 14-08 ~18:10Z) | ✅ merged | ✅ live | Hasan's post-merge look still welcome |
| ✅ **INCIDENT 15-08 14:37Z → RESOLVED 16:31Z: GATEWAY VM DISK FULL → BOTH FIX SESSIONS DOWN ~1 h 40 min.** The never-rotated FIX wire log (`/opt/fix-gateway/data/log/FIX.4.2-FHINPLAY01-TZFIXORDQA.messages.current.log`, **8.7 GB**) + `/var/log/syslog` (3.5 GB) filled the 19 GB root disk; quickfix's next logon (14:38:16Z) failed on `unable to write to file: .../store/...header: no space left on device`, its reconnect loop died, and **OE + MD sessions to tZERO have been logged off since ~14:52Z**: the gateway locally rejects every order (`SESSION_DOWN`, ~500+/book), the engine's `mm.state` still says all 180 `stable` (it is quoting into a void), the venue holds ~3,806 unmanaged MM resting orders at pre-drop prices, the app's books are frozen. Nothing alerted. **DONE (George's ruling: archive, do not delete):** all four logs streamed gzip → **`gs://inplay-fix-gateway-logs/2026-08-15-disk-full/`** (1.8 GB gz, verified readable from 29-05) → truncated in place, journald vacuumed, old `gateway-go.bak-*` binaries pruned → disk 100% → 30%. **PREVENTION INSTALLED + PROVEN:** rotation ONCE a day at **08:00 UTC** (George's rule: never in a game window — the copy + gzip + upload is latency on the order path; 04:00 EDT/03:00 EST, clear of the 00:01 ET session reset), configs in `/etc/logrotate.fix-gateway.d/` (OUTSIDE the system timer's 00:00 UTC = 20:00 ET run), `nice/ionice`, `copytruncate`, no size trigger; rsyslog re-cut the same way; `/usr/local/sbin/ship-rotated-logs.sh` (`lastaction` hook: gz → `gs://inplay-fix-gateway-logs/<host>/<Y>/<M>/`, local delete on success); the ONE exception is `/etc/cron.hourly/fix-gateway-disk-guard` (≥ 85% used → force a rotation now, because a full disk kills both sessions); + Cloud Monitoring alert **"VM root disk > 80% used"** (policy `4198694945183575391` → the security-alerts email). Repo: gateway **PR #7** (`CLAUDE.md` storage section + `deploy/vm/` reference copies). ⚠ **Honest error, recorded:** the backup prune used a lexical sort and removed the two NEWEST binaries too — `gateway-go.bak-124991e-pre-ttl` (the TTL rollback) and `gateway-go.bak-005fdd8`. Both are rebuildable from git (`main@124991e`, `005fdd8`); until rebuilt, the TTL deploy has no on-VM rollback binary | gateway VM (ops) · `inplay-fix-gateway-go` **#7** (docs) | ✅ archive + rotation live on the VM · #7 open | ✅ **RECOVERED 16:19–16:31Z on George's go ("redeploy the gateway? Or restart with these changes") — the ordered ceremony, an EMERGENCY cutover (no game live; the maker was not quoting anyway):** taker halt (journalled, 0 cancels — session was dead) → `snt-1` stop → engine SIGTERM clean stop tick 95,430 → `systemctl restart fix-gateway` (binary UNCHANGED `main@a41e540`) → **both sessions logged on in 5 s** (logon 14 ms), 180 MD symbols → `cancel_all` swept **3,329** stale MM orders (every venue book verified EMPTY after) → engine **supervised31/CFG-0029** (`main@ed921ca`, fresh journal, replayed 0, 1,580 instructions / 180 books, publisher ON) → taker **SNT-CFG-0021 / snt18** (`step4b-wash@5b10d68`, env backup `env.bak-2026-08-15-cfg0020`, booted AUTO, filling, boot rebase adopting venue truth per book: EAGL +1,537 · SCGC +1,293 · HOUC −429 · SACS −567 · ARKR −633 …). Verified: two-sided books at fresh prices on the venue, dead-man armed/unlatched, MM open ~1,573 climbing | ⚠ Two follow-ups: (1) **317 gateway-side PHANTOM orders in `PENDING_NEW`, all stamped 14:37:39Z** (the second the disk filled) — never reached tZERO (venue books empty post-sweep) but the tracker holds them and every sweep skips them `NOT_CANCELABLE`; zero risk, inflates the gauge; fix belongs with the reason-0 phantom PR (age out PENDING_NEW rows on a dead session). (2) Rebuild the two deleted rollback binaries from `main@124991e` / `005fdd8`. ✎ **Ceremony fault #2, recorded (the serious one): the background `cancel_all` loop outlived its step — rounds 4/5 fired 16:28:21Z + 16:31:07Z AFTER the fresh engine was up and swept the new book twice** (`cancels_sent` 3,329 → 6,868); the engine re-stood both times (two CONVERGE_STALE bursts), 16:38Z: 1,617 resting / 180 two-sided / all stable / no alarms. Runbook lesson: sweep loops run FOREGROUND with an explicit exit before the engine starts, and never gate on the phantom-inflated `open_orders` gauge. ✎ Ceremony fault #1: the taker's FIRST start ran on the OLD env (a `sed` with a `#` delimiter failed silently) — it replayed snt17 and booted HALTED on the journalled halt, traded nothing, stopped within seconds, env patched by python, restarted clean. Session: sessions/2026-08-15-b-n40-suspension-live-investigation (addendum) |
| **Gateway: retire tracker rows on `reason=0 + "ORDER DEAD"` (the reason-0 phantom fix)** — tZERO answers cancels of already-dead orders with CxlRejReason=0 text `ORDER DEAD[DMA]`, but the gateway retires only on reason=1 (UNKNOWN_ORDER), so dead rows re-cancel on every sweep for ever. Found live 15-08: ~702 phantoms froze the pre-swap sweeps; **~211 survive the restart** (rehydrated from Redis) and inflate the MM open-order gauge + take the boot-grace dead-man's 203 cancels, all refused. Fix shape: symmetric with the `MarkGoneAtVenue` reason-1 path, gated on the ORDER DEAD text (reason 0 alone proves nothing — the 15-08 session's overloading finding) | `inplay-fix-gateway-go` — no PR yet | 🔴 not built | — | A quiet slot; then the ordered ceremony + George's go. Source: sessions/2026-08-15-gateway-ttl-deploy |
| ~~**Gateway forwards tag 9383 on exec events**~~ ✅ **DEPLOYED 14-08 11:48Z in the bundled deploy** (`main@124991e` binary; "OE exec position" lines prove parsing) — see Landed | `inplay-fix-gateway-go` **#3** | ✅ merged | ✅ live | — |
| ~~**Step 4 phase B — the converger on its own task**~~ ✅ **DEPLOYED 14-08 11:51Z** (supervised28/CFG-0026, task mode live) — see Landed | `inplay-market-maker` `feat/always-quoting-step4b` | ✅ | ✅ live | — (honest scope stands: does NOT fix the ~35% missed sweeps) |
| ~~**Taker boot rebase**~~ ✅ **LIVE from SNT-CFG-0019, 12:10Z** (CFG-0018 ran `snt-checkout@772e79c` — rebase absent there; see the corrected row below) | `feat/always-quoting-step4b` @`db45300`, running as `step4b-wash` @`5b10d68` | ✅ | ✅ live | — |
| **Quantity grid DROPPED — raw share counts, was 500-blocks** — one dictionary row, `qty_increment` 500 → **1**; every displayed size was a multiple of 500 (both rounding steps quantize on the row) so the book read as machine blocks. A first cut moved it to 100; George's same-session ruling: ANY visible grid reads as an inactive book — raw integers (12,433 · …) read like partial-fill remainders. Mechanism untouched (seeded VF, half-down, clamps); §5.8 materiality stays 500 sh; one row restores any grid. ⚠ Book-visible → Edwin round (E31 batch). Decisions 2026-08-15 | `inplay-market-maker` **#36** | ✅ **MERGED to main 15-08 (`2c74886`)** — 885 tests, ruff + mypy clean | 🔴 not deployed (⛔ the 14-08 freeze; the running engine quotes the 500 grid until the next cutover) | The next quiet-window engine cutover (R11) + CFG bump |
| ⭐ **Publisher: never retire a finished game (N40) — QUEUED FOR PRODUCTION on George's 15-08 ask ("put a build deploy thing for this so that we can fix it")** — settle watch at the live rate (600 s → 2 s), post-game window 1 h → 2 h, then OVERNIGHT cadence forever instead of `None` | `inplay-sportradar-service` **#38** — `fix/mm-publisher-post-game-keep-polling` @`751efb6`, cut from `main@f8c8aef` (independent of the still-open #37) | ✅ built — 598 tests, ruff clean | ✅ **TESTING pool since 14-08 ~12:55Z** (George: "merge to testing"; cherry-pick onto `testing@daf5604` → `d492dcb`, run 31802187471; verified 12:56Z — probability updates resumed in the supervised28 journal for 71548096/104/106 + the dup id) · 🔴 **production pool still on the old code — and it RECURRED 15-08 ~02:10Z**: the 14-08 real slate's six books (COMM/BUCC/JETS/DOLP/BRON/FALC) staleness-suspended ~20 s after each game's last reading (02:07–02:10Z), live-verified in `mm.state`. The 02:50Z supervised30 fresh journal dropped the suspensions and the six re-quote at pre-final RPs; **tZERO's FAILSRISK 3% band is what blocks the mispriced sides** (COMM sells ×78 · DOLP buys ×74 · BUCC sells ×4 rejected). Session: sessions/2026-08-15-b-n40-suspension-live-investigation | **The deploy path: (1) #38 → main as a clean PR (the #37 trap stands — do NOT ride the testing promotion), (2) prod release of the publisher pool.** ⛔ Blocked by the 14-08 deploy freeze — needs George's explicit lift for the PUBLISHER (his 15-08 "fix it" ask is the intent; the lift must be explicit). ⚠ Scope: discovery only re-adopts TODAY's-dated games — the 08-13-dated CIN–DET and PIT–GB stay feedless, so **BENG/LION/STEE/PACK still ride seed until the engine-side N40 fix**, and the already-retired 14-08 slate stays feedless too: #38 stops the NEXT game night's suspensions, it does not resurrect these six. Source: sessions/2026-08-14-gateway-watch-and-game-end-forensics (addendum) |
| **Engine-side N40 — the game-end lifecycle** (the real fix): consume `status` from the re-offers → journalled per-book game-final event → explicit policy (suspend-pending-settlement WITH re-open / freeze / reseed — the E11 call) → settlement pricing (George's 🟡 RP + (result − kickoff p) × $5, which keep-polling delivers for free IF the N22 anchor survives cutovers = #32 ANCHOR_SEED deployed) → terminal backstop for never-final games → alarms ("SUSPENDED > N min", "quoting while bound game closed") | `inplay-market-maker` — no PR yet | 🔴 not built (fix shape recorded in open-questions N40) | — | The policy call first (George + E11 settlement definition from Edwin); #32 ANCHOR_SEED deploy is load-bearing for the settlement pricing; then a design session (activity states × market states × RP lifecycle — the one George asked for 14-08) |
| ~~**THE BUNDLED DEPLOY**~~ ✅ **DEPLOYED 14-08 11:46–11:51Z** (George's go, daylight window, no live games) — see Landed | — | — | — | — |
| ~~**Taker T-S05 reconcile halt — IPTCCLEM**~~ ✅ **RESOLVED 00:54Z** (George's ruling: trust the venue) | — (operational) | — | `SNT_FLOAT_OVERRIDES` CLEM 3812→3794 (`env.bak-clem` kept), taker restarted (booted halted, mark held) + resumed; filling, no re-halt | — (the permanent fix is the boot-rebase row above) |
| **No price updates for IPTCBENG (Bengals) + IPTCLION (Lions)** — reported by George in the 14-08 live test: the app shows no quote movement for these two while other symbols move | — (operational, not code) | — | ✅ **TRIAGED 14-08 ~01:20Z — the win-prob path is HEALTHY end to end; the feed is NOT dead.** All four stages verified live: publisher polls all six games; `SR_PROBABILITIES` fresh for 4416/4419 (last reading accepted 01:11:25Z, SR stamp 01:11:14Z, value moving 0.798→0.848); runtime binding correct (both books priced + requoted, ~2,600 acks each in 17 min, zero rejects, no suspensions); freshness CURRENT | **Root cause is the mid-game p_ref rebase, compounded by a decided game.** Tonight's cutovers each took a FRESH journal (replayed 0): supervised25 → supervised26 (first event 23:40:10Z) → supervised27/CFG-0025 (first event 00:19:53Z). On each boot the valuation engine first sees an in-play game and freezes `p_ref` at the CURRENT probability (`[late-arrival]`, `mm/valuation/engine.py:331`) — supervised27 froze BENG at **0.866** instead of the 23:03Z kickoff anchor **0.711**, so the live adjustment is −$0.09 instead of +$0.69; the accumulated in-game move is ERASED at every cutover and the book snaps back to near-seed (BENG seed ≈$72.49, venue mid 72.05 observed). CIN–DET is a blowout (0.848/0.152, saturated) so post-rebase the price has ~±$0.10 of legal movement — looks frozen; close games (PIT–GB 0.563, NE–IND 0.475) keep moving visibly, which is why only these two were reported. ⚠ Systemic: **every fresh-journal deploy during a live game violates the N22 freeze-at-kickoff ruling and discards live price history** on all live books. George ruled BOTH: the operating rule (**R11**, landed Phase 0 — no maker cutovers while games are live) and the carry fix (**`ANCHOR_SEED`**, built 14-08 as MM #32 — the row at the top of this table). Neither is deployed yet |
| **MISSED-SWEEPS FIX, step 1 — the engine-cost measurement** (~half a day): an instrumented run breaking tick time down per stage (readings drain · venue drain · 180 cycles · converge · encode) and per event, on the rig via the A2 replay at 10× — no live game needed. Output: ms-per-ack, ms-per-cycle, the real binding stage. Also sizes the drain-cap re-size owed since group commit (venue cap must RISE toward ~1,050 acks/tick) | `inplay-market-maker` — instrumentation branch off the deploy lineage | 🔴 not built | — | A quiet slot; nothing external. **Prerequisite for steps 2–3 — measure before optimizing** |
| **MISSED-SWEEPS FIX, step 2 — the design fixes** (1–2 sessions, language-agnostic — a Go engine would want both): (a) **de-phase the dwell waves** — jitter republish schedules so acks stop arriving in 100–200-event clumps; (b) **the incremental sweep** — a book whose inputs are unchanged since the last sweep provably yields the identical output (pure functions), so skip recomputing it; §3.1.4 semantics preserved, replay equality re-proven by the A2 drill. (b) is the big win and touches the machine's heart — reviewed, not rushed | `inplay-market-maker` | 🔴 not built | — | Step 1's numbers (they say whether (a), (b) or both pay) |
| **MISSED-SWEEPS FIX, step 3 — Python speed work** (1–2 sessions, dies with any Go port): hot-path optimization ONLY where step 1's profile says it pays (ack fold, Decimal churn); optionally a faster VM core (single-thread bound). Target: NFL-scale miss rate ~0% | `inplay-market-maker` / VM sizing | 🔴 not built | — | Step 1's profile. ⚠ Honest ceiling: NCAA Saturday (~2,500 events/s) likely exceeds Python regardless — that is **the Go decision** (weeks-scale rewrite + re-proving replay equality; parked 04-08, tonight's evidence reopens it as a season-2/NCAA call) |
| ~~**Taker lineage confusion**~~ ✅ **RESOLVED 12:10Z — CFG-0019 deployed.** ✎ CORRECTED STORY: the CFG-0018 taker imported from `~/snt-checkout/src` @ `main@772e79c` (PYTHONPATH is the truth, not the venv path) — so the wash guard **never regressed**; instead the **boot rebase never actually ran** in CFG-0018 (the 11:51Z "ACTIVE" receipt was wrong, corrected in the Landed row). Fix: `feat/always-quoting-step4b@5b10d68` (main merged — BOTH features) checked out in `snt-checkout` (branch `step4b-wash`), **CFG-0019 / journal snt16**, floats = env + snt15 drift (123 books), env backup `.bak-cfg0018`; filling from 12:09:59Z, no halts. Lesson for the runbook: the taker's code = `snt-checkout` (PYTHONPATH), never the ExecStart venv path | `inplay-market-maker` | ✅ | ✅ live | — |
| ⚠ **PR #37 IS A TRAP — do not merge casually**: its head IS `origin/testing` (65 commits, +7,544/−371), so merging promotes ALL of testing to main. The true one-hunk universe-filter hotfix (`hotfix/mm-publisher-universe-filter@d877b26`, cut from main HEAD, RUNNING in prod) has NO PR at all | `inplay-sportradar-service` | — | prod runs the hotfix branch; main still carries the regression | **George's ruling**: close #37 + open a clean one-hunk PR from `d877b26`, or deliberately promote testing (Hasan's call too) |
| ⭐ **TAKER LIVE RATE — one print a second (George's 15-08 ruling), the arrival-clock fix, the portfolio cap.** Multipliers PRE_KICKOFF 6 → **20** · LIVE 75 → **400** · POST 4 → **20** (one print per book every 20 s / 1 s / 20 s; OVERNIGHT untouched at 9/h); rates now env-tunable as intervals (`SNT_INTERVAL_{STATE}_S`). ⭐ Found while building: the arrival clock rescheduled from the TICK's time, +27% long at a 1 s target on a 0.5 s tick (measured) — fixed (`schedule_after_arrival`, one tick of backlog max, no catch-up bursts); tick 0.5 → **0.25 s** (`SNT_TICK_S`); realised LIVE gap 1.012 s. `SNT_MAX_ORDERS_PER_S` portfolio cap built, **ships OFF** (each taker fill = a maker exec ack; a Sunday slate at 1 s ≈ 20/s ≈ +20% of the maker's tick — **N44, George's call**). Decisions 2026-08-15e · session 2026-08-15-taker-live-rate | `inplay-market-maker` **[#40](https://github.com/Novosapien/inplay-market-maker/pull/40)** (`feat/snt-live-rate`, off `main@2c74886`) | ✅ built — **896 tests** (885 + 11), ruff + mypy clean | 🔴 not deployed (⛔ the 14-08 freeze; the running taker SNT-CFG-0020 still fires every 5.3 s in LIVE) | Review + George's merge go; then the standard taker cutover (halt → stop → CFG bump → floats from the RUNNING env + snt17 drift → start) on the freeze lift, in a quiet slot (no live games — the ceremony stops the taker). Rig run at the LIVE rate first (TT9 in the taker test plan). Edwin confirms the intervals in the E41 round; N44 (the cap) before the first Sunday slate |
| **Taker shorts** (side 5, T-O10, off by default) | `inplay-market-maker` **#15** | ✅ merged | ✅ in the running binary; feature **OFF** (`SNT_SHORTS` unset) | QA test (TT8) needs a zero-float book — JETS after Rob resets its band; E26 (depth rules), T16 (entitlement) |
| **Maker shorts** (N34 — ask ladder side-flip at minting) | `inplay-market-maker` | 🔴 not built | — | The MM session (handed over 11-08, vault decisions 2026-08-11d) |
| **Vault docs branch** | `inplay-vault` `docs/t0-plain-english-guide` | ⚠ 5 sessions of working docs UNCOMMITTED | — | George's go to commit + push |
| ⭐⭐ **FIX-SET CB4 (F1c/R4) — the ack drain was ONE SCAN, and removing it met AC4's miss gate.** `VenueEngine._stamp_and_prune` walked every order in every book on EVERY venue event, re-parsing each terminal order's timestamp against the retention cutoff. Two derived indexes replace it — `_unstamped` (terminal-but-unstamped, maintained by `_put`) and `_expiry_queue` (a heap ordered by PARSED stamp, so the out-of-order venue stamps of `[monotonic-at]` expire exactly as the old per-order test made them). Both are DERIVED — absent from `state()`, rebuilt by `restore()` — so **no checkpoint byte moves** and AC9 holds by construction. ⭐ **MEASURED ON THE RIG, adjacent pre/post arms, six-game 1×, n2-standard-2:** a `perf_counter` around the function shows it was **7,225.8 µs/call = 98.4% of the per-ack cost**, now **6.72 µs = 2.3%** (**1,075×**). Whole-loop: **ms/ack p50 7.346 → 0.298 (24.6×)**, **missed-sweep ratio 28.755% → 0.000%** (AC4 wants < 0.5%), late ticks 43.43% → 0.00%, tick p50 411.85 → 32.31 ms, and **the venue drain falls from 97.7% of the tick to 47.5%** — it is no longer the tick. 10× moves the same way (3.953 → 0.254). ✅ **R9 PASSED at rig scale:** two folds of the measured run's own 226 MB / 224,876-event journal agree byte for byte on 40,244,373 bytes of state, 0 publishes. 🔴 **AC4's OTHER clause fails: 7 DRAIN_CAPPED ticks against "zero"** — but they are the boot re-stand (1,598 standing instructions ÷ 512 cap ⇒ ≥4 ticks; `acks_per_tick` max 512 vs p99 130; the count is 5–7 on EVERY arm ever run while the miss ratio went 52% → 0%). **CB4 reads the clause as mis-specified, not failed — George's call.** 🟡 **Drain cap re-derived (ANALYSIS, no dictionary row):** at 0.298 ms/ack p50 the rule `cap ≤ 300 ms ÷ ms-per-ack` gives **≤ 1,006**; the standing **512 cap is no longer dangerous** (worst case **3.76 s → 153 ms**, inside the 500 ms tick for the first time); the **1,050** NCAA figure needed ≤ 0.286 and we measure **0.298 — 4% short at p50**, ~2× at p90, and a cap must survive p90, so **no change proposed**. ⭐ **Go decision datum moves: ~101 acks/s on one core → ~1,700 (p90) – 3,400 (p50)**, so NCAA Saturday's ~2,500/s sits BETWEEN them instead of 25× outside. 🛑 **AND THE MEASUREMENT LESSON, which outranks the perf one:** the FIRST set of arms all loaded `~/mm/src` whichever tree they were launched from — `six_game_workload.py` put the repo ROOT on `sys.path` under a `src/` layout, so `import mm` fell through to a venv whose `.pth` had been copied between trees; the fixed engine was on disk and **never imported**, and a whole night of "before/after" was **before vs before**, with plausible numbers throughout. Caught by a `perf_counter` probe emitting ZERO lines. Fixed (`MM_REPO/"src"` first) and made loud: `profile.json` now carries `run.engine` and the summary prints it beside the numbers. ⚠ **This rig drifts ~1.7× day-to-day on identical code** (9.893 vs 6.149 ms/ack, same engine) with a **~4% adjacent-arm noise floor — cross-day rig pairs are inadmissible**, which retroactively softens `profile-cb1.md`'s absolutes (its 96–98% drain SHARE is what reproduces). Conventions in `profile-cb4.md` §6 | `inplay-market-maker` **#43** (base `main`, scripts only — the `sys.path`/provenance fix + `cb4_scan_cost.py` + `cb4_replay_check.py`). **#39 CLOSED**: it was stacked on the now-merged `fix-set/cb1-profile-workload` and the engine fix reached `main` independently. ⚠ **The measurement still describes `main`** — `main`'s `_stamp_and_prune`/`_index_terminal`/`_put`/`process` are **byte-identical to the measured tree** (verified), and `cb4_scan_cost.py` run against `main` reports 0.0003 ms/call | ✅ built — **1,227 tests** on `main` + the scripts, ruff + mypy clean (73 files); drill PASSED at rig scale | 🔴 not deployed (deploy freeze; measurement only, no `src/` change in #43) | **George: (1) the DRAIN_CAPPED clause, (2) whether the venue cap moves, (3) the Go decision re-taken against 0.298 not 9.893** |
| **Protocol drills TT4 (kill mid-window) · TT6 (rig replay of a full game)** | — | 🔴 not run | — | A quiet slot; no externals |

## Landed (most recent first)

| Change | Repo / PR | Live since | Coordinates / proof |
|---|---|---|---|
| **THE GATEWAY TTL DEPLOY — request-registry TTL (#6) + `GET /orders/mm` (#5)** — an in-flight 35=F/35=G the venue never answers now expires at 30 s (5 s sweep) with a local `REQUEST_TIMEOUT` reject, ending the permanent REQUEST_IN_FLIGHT wedge (the app's "stuck Updating…" report; app half = inplay-app `d0e3940`, prerelease OTA) | gateway `main@a41e540` | 15-08 02:47–02:51Z (~4.5 min outage, post-slate window, R11 honoured; George's explicit go superseding the 14-08 freeze for the gateway) | Ordered ceremony held: taker NATS halt as `admin` (3 cancels) → `snt-1` stop → engine clean stop tick 19,607 → 4× cancel_all (~1,800 cancels; the frozen 702 were reason-0 phantoms, see In flight) → binary swap (backup `gateway-go.bak-124991e-pre-ttl`, new sha `737a6888…`) → **engine supervised30/CFG-0028** (code unchanged `main@ed921ca`, relaunched from captured environ) → **taker SNT-CFG-0020/snt17** (code unchanged `step4b-wash@5b10d68`, env backup `.bak-2026-08-15`), booted AUTO, boot-rebased NTMG +1,547 / NCWP −521. Verified: both FIX sessions on, 180 MD symbols, MM re-quoting (1,816 climbing), heartbeat silence ~220 ms, dead-man armed, no new-binary errors. ⚠ ~12 non-MM user rows rehydrated (was ~20 pre-restart) — likely venue-dead phantoms, reconcile check owed. Session: sessions/2026-08-15-gateway-ttl-deploy |
| **THE BUNDLED DEPLOY — gateway #3 + converger task + boot rebase** | gateway `main@124991e` · MM/taker `feat/always-quoting-step4b@db45300` | 14-08 11:46–11:51Z | Ordered ceremony honoured (taker halt/stop 11:47 → engine clean stop tick 67,381 → double cancel_all → gateway binary swap → engine → taker). **Gateway:** `/opt/fix-gateway/gateway-go` = the #3 build (backup `.bak-005fdd8`), dead-man 10,000, both FIX sessions on, "OE exec position" lines prove 9383 parsing live. **Engine: supervised28/CFG-0026**, fresh journal, 1,664 instr/180 books, converger TASK mode live (one boot-transient CONVERGE_STALE, backlog ≤2 after). **Taker: SNT-CFG-0018, journal snt15**, floats = env base + snt14's net drift (20,190 lines folded; CLEM check 3794+44=3838 ✓), env backup `.bak-cfg0017`; zero halts, filling from 11:51:31Z. ✎ CORRECTED 12:10Z: CFG-0018 ran `snt-checkout@772e79c` (wash guard YES, boot rebase NO — the "rebase ACTIVE" claim here was wrong); both features live only from **CFG-0019** (next row in In flight → resolved). ⚠ ~695 resting non-MM orders (users + the ~10:15Z STX batch, T19) are NOT ours and rest untouched |
| **Dead-man window 4 s → 10 s** — ends the game-night fire loop (~130 sweeps at silence 4.0–4.7 s) | env row on the gateway VM · default bump in `inplay-fix-gateway-go` **#4 — ✅ MERGED to main 14-08 ~18:10Z (George's go)**; live behaviour unchanged (the env row already carries 10000) | 14-08 00:19Z | `MM_DEADMAN_TIMEOUT_MS=10000` in `/opt/fix-gateway/.env`; binary UNCHANGED (#3 still undeployed). Ordered sequence honoured; engine now **supervised27/CFG-0025** (fresh journal, 1,618 instr); zero fires since, beat silence ~1 s peak. Decisions 2026-08-14 |
| **Publisher universe-filter hotfix** — `sr:competitor:` ids pass discovery; the publisher adopts and polls games for the first time ever | `inplay-sportradar-service` — hotfix branch (prod) + `testing` cherry-pick + **PR #37 → main (OPEN — merge or the next main deploy regresses it)** | 13-08 ~22:38Z (prod) · ~22:44Z (testing) | Prod pool: `hotfix/mm-publisher-universe-filter` @`d877b26` (cut from running SHA `f8c8aef`), run 31750593932. Testing pool: `testing@daf5604`, run 31751010981. Both verified polling `71548090/92/94` at 15 s; testing also covers the 08-14 late slate. Session 2026-08-13-e |
| **Wash guard** — never send against an own resting IOC remnant | MM **#29** | 13-08 18:01Z | Taker `SNT-CFG-0017`, journal `snt14`, `main@772e79c`. 46 fills/2 min post-cutover, zero alarms. Venue wash flag stays ON (ruling 13-08) |
| **T-F07 fetch-age freshness** — staleness prices `Fetched-At`, not delivery | MM **#28** | 13-08 13:15Z | In the running binary since CFG-0014. Kills restart-re-derives-LIVE (3 occurrences 11/12-08) |
| **Exec-borne T-S05 (taker half)** — prefers the exec's own 9383 | MM **#28** | 13-08 13:15Z | In the binary; **primary path INERT until gateway #3 deploys** — fallback = today's proven behaviour |
| Taker unattended deploy + AUTO states + NATS grants + Secret Manager | MM #12/#14 | 11-08 | `snt-1.service` on the MM VM; `snt-taker` NATS user; stream `SR_PROBABILITIES` |
| mm-publisher worker (the taker's schedule feed) | sportradar-service #8/#10 | 11-08 | Cloud Run pool `inplay-mm-publisher`, 1 instance; the publisher owns the stream config |
| Reject backoff + cancel-reject drain (engine) | MM #11/#13 | in `main` 11-08 | Engine picks them up per its own runs (operating session's coordinates) |

## Standing facts a deployer must know

- ⛔ **R11 (George, 14-08 — spec Phase 0, effective NOW): no maker
  cutovers while games are live.** Live = any universe game between
  kickoff and final, or inside its pre-kickoff hour. Emergencies
  allowed — mirror an emergency cutover into the session note. Recorded
  in decisions.md 2026-08-14c + the engine repo's
  `deploy/OBSERVABILITY-REDEPLOY.md`.
- Every taker deploy: **halt → stop → new binary → CFG bump + fresh
  journal + floats recomputed from the RUNNING env + journal drift →
  start.** Floats are positions, not constants.
- Gateway restarts follow the ordered sequence, always.
- Merged ≠ deployed, on every repo — check the running coordinates.
- The operating session cuts the taker's env frequently (CFG-0013 →
  0016 in one afternoon, 13-08); read `/etc/snt-1/env`, never assume.
