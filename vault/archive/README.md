# Archive

> **Rule: nothing in this folder is current.** Every item here is a dated
> snapshot or a superseded one-off, kept because it is history, moved here
> so it can never be mistaken for a working document. Nothing is deleted —
> if an item mattered, its replacement is named below.

| Item | What it was | Superseded by / why archived |
|---|---|---|
| `project-status-synthesis-31-07.md` / `.html` | The 31-07 cross-project status synthesis | A dated snapshot; ongoing state lives in the component docs and session notes |
| `project-status/` | The rendered per-area status pages behind the 31-07 synthesis | Same snapshot, page form |
| `project-status.zip` | The zipped copy of the same pages | Redundant with `project-status/` |
| `george-architecture-doc.md` | The 09-05 technical-architecture draft | Predates the v1.3 spec, the MM build and the real VPC; current truth: `architecture/` + `components/market-maker/build/` |

Component-local archives follow the same rule (e.g.
`components/market-maker/archive/`).
